-r requirements.txt

# 50 起不再提供 Intel macOS wheel，开发锁需同时支持本地 x86_64 与 CI Linux。
cryptography==46.*

# 静态类型检查：仅开发/CI 使用，不进入生产镜像。
mypy==2.3.*
types-python-jose==3.5.*

# 既有异步 SQLite 测试 Fixture 所需驱动，仅测试使用。
aiosqlite==0.22.*
