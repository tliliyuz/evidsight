# Changelog

本文记录 EvidSight 产品规范、架构、契约和实现的可审查变化。格式参考 Keep a Changelog；“已确认设计”不等同于“已经实现或发布”。

## [Unreleased]

### Added

- 纠偏 3B「知识中心」后端契约增补（落实 ROADMAP「复核 3B：知识库列表、详情、Drawer」的后端数据源部分；负责人批准按任务「增加明确索引状态列，后端无权威聚合索引状态则先补 API/OpenAPI」执行。前端列表/详情视觉 RED/GREEN 后续推进）。`KnowledgeBaseResponse` 增补两个向后兼容可空可选字段：① `index_status`（`ready|updating|recovering`，映射既有 `knowledge_bases.index_status` 索引发布锁列，ADR-007 权威聚合索引状态，前端据此渲染列表「索引状态」列，不得根据 `chunk_count` 猜测）；② `owner_username`（owner 用户名，供详情 Hero「创建者」展示与列表辅助信息）。新增 `resolve_user_display(db, user_id)` 一次查询返回 `(platform_user_id, username)`，替换 KB 响应构造处的 `resolve_user_uuid`（service `create_kb`/`list_kbs`/`list_visible_kbs`/`update_kb` 与 v1/legacy 详情六处填充，跨用户列表按去重 user_id 解析一次）；同步 External OpenAPI `KnowledgeBase` schema（两字段可空、不入 required）与契约测试（`_make_kb_response` 补字段、`test_get_success_200` 断言序列化、`resolve_user_display` 单测、service/API fixture 同步 `_make_first_result`）。验证：RED 确认（`KeyError: index_status` + `ImportError: resolve_user_display`）；受影响 **173 项**全绿、knowledge 快速单元 503 通过（另 1 项存量漂移失败：`test_chat_generation_service.py` fixture 的 `SimpleNamespace` 缺 `doc_uuid_map`，切片 4 增补后未同步，与本次改动无关）、ruff check/format 通过、mypy knowledge 213 文件零错误、OpenAPI 静态校验通过。ADR 检查 1–8：否（两个可空可选字段投影，对齐纠偏 2 `source_strategy`/`progress` 先例，不改变服务边界、权限模型、公共契约语义、数据生命周期或生产机制）。（2026-08-10）
- 命令入口与命令面板（落实原型顶栏 `⌘ 搜索或执行指令 ⌘K`，负责人指示「实现+补规格，先补文档」；先补 FRONTEND §3.4 与 UIDESIGN §7.21/§7.22 规格再实现）。① 顶栏右侧（运行任务 Chip 之后）常驻命令入口按钮「搜索或执行指令」，携带 `⌘K` 快捷键提示；`⌘K` 或点击打开命令面板，Escape 或外部点击关闭，关闭后焦点返回触发器。② 新增 `CommandPalette` 组件（combobox 模式）：命令入口按钮被点击/⌘K 后**原位展开为搜索输入框**（不弹出独立面板，输入框尺寸与按钮一致 `min-width:220px`/高 32px，聚焦态 `--es-focus` 边框），空输入不显示列表，输入有匹配后才在输入框下方弹出过滤后的命令列表（`position:absolute; top:calc(100%+8px); left:0; right:0`，`--es-surface` + 1px 边框 + `--es-radius-major` + `--es-shadow-popover`，不加遮罩）；`↑/↓` 移动、`Enter` 执行、输入有值无匹配展示「无匹配命令」，Escape 或点击输入框外部关闭并恢复命令入口按钮；v1 命令范围限定为六项页面导航 + 「创建知识库」，不执行任意指令、不跨数据源全文搜索；复用 `useOverlayFocus` 管理初始焦点/Tab 循环/Escape。③ 新增 5 项单元测试 + 3 张视觉基线（打开输入框浅/深、输入弹出过滤列表浅）。验证：RED 确认（组件缺失致测试 collection 失败）；vitest 全量 **124 项**（新增 5 项）、ESLint、Prettier、TypeScript/Vite build、Design Token（40 Token/49 文件）与视觉基线门禁、Playwright 全量 **14 项**（workbench 9 + landing 5）全部通过。ADR 检查 1–8：否（落实原型与既有 FRONTEND §4.1「命令面板状态」预留，纯前端新能力，不改变服务边界、公共契约、权限模型或数据生命周期）。（2026-08-10）
- 纠偏 2 工作台视觉 RED/GREEN（落实 ROADMAP「纠偏 2：AppShell 与工作台视觉 RED/GREEN」的明暗主题与三态截图部分，对齐 UIDESIGN §5.5/§5.6/§7.2 与跟踪版原型 `02-workbench`；行为与结构已于 `9c1a304` 提交）。① 工作台 Route Surface 容器化：`.workbench` 由直接落在 Canvas 上的透明区改为白色 Route Surface（`--es-surface` + 1px `--es-border-default` + `--es-radius-major` + 内边距，不使用阴影），页面标题与主次列整体进入白色大容器，四周保留浅灰 Canvas，落实「浅灰 Canvas → 白色 Route Surface → 灰色 Inset Panel/连续 Ledger」层级（此前为「灰色整页 + 白色卡片」，层级相反）。② `.workbench-launcher` 与 `.active-mission` 由白色改为 Inset Panel（`--es-surface-muted` + `--es-radius-panel`），launcher 内部两个白色 Action Card 保持 `--es-surface` 白底（§5.5 允许 Action Card 出现在 Inset Panel 上）；右栏 Context Rail 的 Hairline 分隔、RECENT RESEARCH Ledger 与 RECENT KNOWLEDGE Hairline 列表不变；深色主题只替换 Token，结构与边界数量不变。③ 新增 `tests/e2e/workbench.spec.ts` 视觉验收：普通用户空态 / 有最近研究和知识库 / 有运行任务 三态 × 浅色/深色 六张 1280×720 截图基线（`maxDiffPixels: 5` 容差沿用），通过 `addInitScript` 注入 `evidsight_csrf` Cookie 建立 restore 登录态并 mock refresh/me/research/knowledge 端点（`status=running` 查询区分运行集与最近列表），`page.clock.setFixedTime` 冻结相对时间；Playwright 配置新增 `timezoneId: 'Asia/Shanghai'` 保证问候文案与「今天 HH:MM 更新」跨机器稳定。行为门禁（六项导航顺序、普通用户不显示管理入口、空态行动入口）由既有 `app-shell.test.tsx`/`workbench.test.tsx` 覆盖。验证：RED 确认（容器化前像素采样 launcher 为白 `#ffffff`、内容区为 Canvas 灰 `#f3f4f6`，与规范相反）；GREEN 后像素采样 launcher `#f8fafb`（Inset Panel）、容器内 `#ffffff`、容器外 Canvas `#f3f4f6`，深色对应 `#151a27`/`#0d111a`/`#07090f`；vitest 全量 118 项、ESLint、Prettier、TypeScript/Vite build、Design Token（40 个 Token/48 文件）与视觉基线静态门禁、Playwright 全量 11 项（workbench 6 + landing 5）全部通过。ADR 检查 1–8：否（落实既有 UIDESIGN/FRONTEND 与 ADR-004，纯前端视觉容器化与测试基线，不改变服务边界、公共契约、权限模型或数据生命周期）。（2026-08-09）
- 纠偏 2（AppShell/工作台）视觉审查后的 4 项修复（负责人指示，落实既有 FRONTEND/UIDESIGN 契约）。① OpenAPI `ResearchTaskListItem.required` 补入 `source_strategy`/`progress`，与前端 `research.ts` 必填类型及服务端实际响应一致（此前 OpenAPI 声明可选而前端绑定必填，契约矛盾）。② 前端 `ResearchTaskStatus` 补 `paused`，工作台 `STATUS_LABELS`/`STATUS_TONES` 增加「已暂停」（warning），对齐后端 `TASK_STATUS_ENUM`。③ 账号菜单补完整键盘交互：方向键/Home/End 循环移动、Escape 关闭并返回触发器、点击外部关闭、打开时焦点进入首项；菜单项结构与原型对齐（标题 + 副标题 + 箭头，主题项显示「当前：X」）。④ 主题选择改回原型两步骤流程（FRONTEND §3.3）：账号菜单「主题选择」打开居中选择卡片，点击浅色/深色弹出「切换主题？」确认卡片并叠加在选择卡片之上（选择卡片保持可见、inert 不参与交互），确认后才应用并持久化；确认卡片取消返回选择、选择卡片取消关闭；确认切换后顶部弹出成功反馈 Toast「已切换为X主题」（role=status，约 2.6s 自动消失）；新增 `ThemeDialog` 组件、全局 Toast 与 `--es-swatch-light/dark` 固定色板 Token（UIDESIGN §4.0 注册）。验证：RED 确认（菜单键盘/主题两步骤/toast/paused 5 项新测试失败）；vitest 全量 118 项全绿、ESLint、Prettier、TypeScript/Vite build、Design Token（40 个 Token）与视觉基线门禁、Playwright 5 项全部通过。ADR 检查 1–8：否（落实既有 FRONTEND §3.3/§10 与 UIDESIGN，契约收紧与服务端实际响应一致，纯前端与文档契约调整）。（2026-08-09）
- 补全跟踪版原型的全前端页面视觉规格（纯文档变更，负责人要求先补规格再继续页面纠偏）。UIDESIGN 新增强制 Surface 语法，逐一定义 Canvas、Route Surface、Inset Panel、Action Card、Metric Strip、Ledger、Context Rail、Reader、Overlay、Admin Surface 与 Full-bleed 的 Token、边界、适用内容、禁止用法及最多两层可见嵌套规则；新增 20 个 manifest 页面 Frame 矩阵，并按路由详细固化入口、工作台、Research、报告、Chat、知识中心及 10 个 Admin 页面在 1280×720 下的主次列、容器、卡片、列表、指标条和 P0/P1 边界；补充登录、上传、来源/切片、Trace、知识库选择与确认六类跨页 Overlay 规格和视觉验收项。工作台现明确为“浅灰 Canvas → 单个白色 Route Surface → 灰色 Inset Panel/连续 Ledger”，右栏为 Hairline Context Rail，不允许整页灰底或右栏卡片墙。FRONTEND 仅交叉引用视觉唯一事实，行为/状态职责不变；没有新增 Token、产品行为、路由、API、权限或交付范围。按 AGENTS.md 纯文档例外不制造业务 RED。ADR 检查 1–8：否（将已批准跟踪版原型事实升格到现有视觉权威文档，不引入新的架构或产品取舍）。（2026-08-09）
- 完成 M4 纠偏 1「入口页与登录抽屉」视觉 RED/GREEN（落实 ROADMAP「视觉反向审计」第 2 项，对齐 FRONTEND §5.1、UIDESIGN §3.3/§4/§6.1/§6.2/§6.6 与跟踪版原型 `#landing`/`#login-drawer`）。① 入口页固定深色品牌叙事：tokens.css 深色作用域把 `.landing` 纳入选择器，入口子树无论工作区主题恒取深色 Token；注册 8 个入口叙事专属 Token（`--es-landing-gradient/grid/grid-mask/trace-secondary/contour/coordinate/proof/cta-gradient`）并同步 UIDESIGN §4.0 注册表。② 入口页复刻原型结构与资产：品牌 Mark、公共导航（产品能力/研究方式/安全与证据）、描边登录按钮；Hero 主标题「看见线索之间/真正重要的联系」替代超大 EvidSight 字标，复用轨道/轮廓/节点 SVG、坐标与权限底注；恢复「进入据见」「沿着证据往下看」与「发起研究」动作；四段纵向叙事（Hero/信息汇聚/研究过程/证据安全）；七阶段流程视觉（规划→生成报告，只展示公开阶段名，不展示隐藏推理内容）；锚点平滑滚动与 `prefers-reduced-motion` 下取消动画；1280px 无横向滚动。③ 登录抽屉补齐统一暗色 Overlay、460px 宽度、品牌头与结构化标题区、统一图标关闭按钮（`aria-label="关闭登录"`）、按 Token 的输入框（50px 高度/背景/描边/焦点环）、「记住登录状态」控件（FRONTEND §5.1 要求，会话持久化由 Refresh Cookie 生命周期决定，不伪造服务端语义）；保留显示密码、统一错误文案（role=alert + `aria-describedby` 关联表单与字段）、提交忙碌态与防重复提交、`returnTo` 目标路由与 `/me` 身份确认；注册入口按 API.md §5 保留，不实现无规格/API 支持的「忘记密码」假入口；注册失败使用独立文案「创建账号失败，请检查账号信息后重试。」，与登录失败文案分离（FRONTEND §5.1 同步）。④ 路由重构为 `LandingLayout`（入口页常驻、抽屉为浮层），关闭后保留入口页滚动位置并将焦点返回登录触发器（对齐 FRONTEND §3.1/§10、UIDESIGN §6.6）。⑤ 新增 `tests/e2e/landing.spec.ts` 视觉验收（深色入口默认/登录抽屉打开/登录失败/提交中四张 1280×720 截图基线 + 键盘焦点顺序、Escape 与关闭后焦点恢复），首轮基线评审在 Playwright 配置固定 `maxDiffPixels: 5` 容差、对填充输入截图掩蔽文本输入以消除跨会话字体亚像素抖动，并将 `test-results/`、`playwright-report/` 纳入 gitignore 与 prettierignore。验证：RED 确认（10 项新测试因目标行为缺失失败）；vitest 全量 104 项全绿（新增入口页 6 项、登录抽屉 5 项、react-baseline 更新）；ESLint、Prettier、TypeScript/Vite build、Design Token（38 个 Token/46 个源码文件）与视觉基线静态门禁全部通过；Playwright 5 项全绿且二次运行稳定。ADR 检查 1–8：否（落实既有 UIDESIGN/FRONTEND 与 ADR-004，纯前端视觉与路由结构调整，不改变服务边界、公共契约、权限模型、数据生命周期或后端机制）。（2026-08-09）
- 落地 EvidSight 全前端视觉纠偏共用的 P0 基础设施，并在当前已实现的入口、应用壳层、知识中心与 Chat Overlay 中首批接入。（对齐 UIDESIGN §4/§6.6 与已固化跟踪版原型）：新增复用原型唯一几何的 `BrandMark`、复用原型 Font Awesome WOFF2 的语义化 `Icon`、统一 `Button` 组件；以 Fontsource 本地交付 Inter、Space Grotesk、JetBrains Mono 指定字体，消除系统字体与网络字体漂移；应用壳层、落地页和登录抽屉接入真实品牌标记，主导航接入统一图标。新增 `useOverlayFocus` 并覆盖确认框、登录抽屉、知识中心三类抽屉及会话重命名框，实现打开后初始焦点、Tab/Shift+Tab 内部循环、非提交态 Escape 关闭、关闭后焦点返回；提交期间保持关闭门禁。按 SDD 新增 5 项验收测试，RED 正确发现共享组件缺失与焦点逃逸；最终 Web 全量 92 项测试、ESLint、Prettier、TypeScript/Vite build、Design Token 与视觉基线静态门禁全部通过；1280×720 浏览器核验确认本地字体/图标加载、品牌标记可见、登录抽屉初始焦点与 Escape 关闭，控制台无 error/warning。ADR 检查 1–8：否（落实既有 UIDESIGN 与 ADR-004，不改变页面业务行为、公共契约、权限模型或服务边界）。（2026-08-09）
- 落地 M4 切片 4 剩余部分「来源卡片联动切片抽屉与实时原文鉴权」（对齐 FRONTEND §5.5/§5.4、API.md §6.2/§12、DATABASE.md §5.5；负责人「继续切片 4 剩余部分」指示，后端稳定身份与前端联动一并落地）。① 后端 Chat SSE `sources` 事件契约扩展：`ChatSourceChunk` 增补 `document_uuid`（`documents.uuid`）与 `segment_id`（`chunks.segment_uuid`，即 location 端点 `location_id`），前端据此进入文档切片抽屉并按 §6.2 实时鉴权展开引用切片，内部整数 `doc_id` 仅迁移期兼容不作为契约。数据流：`RetrievalResult` 增补 `segment_uuid` 并随 Vector（ChromaDB metadata）/BM25（原文与 segment_uuid 同源取回）/RRF 融合/Rerank 穿透到 `build_sources`；`KnowledgePipelineResult` 增补 `doc_uuid_map`（`select Document.uuid`）经 `chat_service`/`sse_stream` 传入；缺省 `doc_uuid_map` 时 `document_uuid` 为 None，旧调用向后兼容。② Chat v1 投影 `chat_v1.py` 将流内 error 事件归一化为 API.md §4 错误信封 `{error_code, message, retryable}`（兼容旧 `code`），前端解析器/状态机相应对齐。③ 前端来源卡片 `SourceCards` 增「进入文档切片」入口，点击以 `document_uuid`/`segment_id` 打开 3B 的 `ChunkDrawer`（新增可选 `initialSegmentId` 自动展开引用切片并实时请求 location；权限撤销/E2015 受限态由抽屉既有逻辑兜底）；sources 不随消息持久化（Message 仅存 content），机器状态机新增 `completedSources`（done 时按 `message_id` 绑定当轮来源，新一轮 send 保留），历史回读后来源卡片持续可见。验证：RED 确认（`RetrievalResult` 缺 `segment_uuid` 致新增 4 项后端测试失败）；后端受影响 232 项全绿、mypy 零错误、ruff check/format 通过；前端新增 4 项（来源联动 1、机器 completedSources 3）、全量 87 项全绿、tsc/vite 构建/ESLint 零错误/Prettier 通过。ADR 检查 1–8：否（向后兼容字段增补，暴露既有稳定 Segment ID 与文档 UUID，同切片 3A 先例，不改变权限模型、服务边界、数据生命周期或鉴权时机；原文鉴权仍由 location 端点实时执行）。（2026-08-09）
- 固化 M4 前端跟踪版原型与 Token/视觉门禁（负责人批准按跟踪版原型纠偏）。① 将 `.superpowers/prototypes/evidsight-web/` 的交互原型净化复制到可版本跟踪的 `resource/prototype/reference/evidsight-web/`，以 manifest 固定 20 个页面、路由、明暗主题截图及 P0/P1 范围；该目录只提供结构、布局、交互入口与资产参考，不是第二套可运行前端，行为冲突仍以 PRD/FRONTEND/API/ADR 为准，且明确 Chat v1 单知识库、P1 Admin、旧 Token、CDN 字体及 mock/hash 路由不得反向进入生产实现。② UIDESIGN 建立唯一 `--es-*` Token 注册表和闭包规则，补齐字体、圆角、遮罩与阴影语义 Token；生产源码禁止未注册 Token、旧原型 Token 及 Token 文件外的颜色字面量，Tailwind 只能映射已注册 Token。③ 新增 `check:design-tokens` 与 `check:visual-baselines` 静态门禁并接入 pre-commit、Web CI 和统一测试脚本；首轮 RED 正确发现 `--es-line`/`--es-radius-sm` 未定义、6 处硬编码颜色，以及 20 张暗色基准图虽扩展名为 PNG、实际为 JPEG，随后改用注册 Token 并将暗色基准转换为真实 PNG，门禁通过（30 个注册 Token、39 个源码文件、20 页/40 张主题基准图）。④ FRONTEND/TESTING/DEVELOPMENT/ROADMAP 固定原型输入顺序、三线并行文件所有权和纠偏顺序；每个切片必须在 1280×720、固定 Chromium、本地字体和确定性 Fixture 下完成明暗主题视觉 RED→GREEN，切片 8 只负责跨页 E2E，不得代替切片级视觉验收。新增 Playwright 固定视觉运行配置，具体页面快照用例随对应纠偏切片按 SDD 落地。纯规范、基线资产与质量门禁变更；ADR 检查 1–8：否（落实既有 UIDESIGN 与 ADR-004，不新增可部署前端、不改变产品行为、服务边界、公共契约或权限模型）。（2026-08-09）
- 落地 M4 切片 4「据见问答与历史」独立部分（对齐 FRONTEND §5.5/§5.6/§7/§12/§13、API.md §7/§12、docs/openapi/evidsight-v1.yaml；只消费已落地的 Chat v1 canonical 协议。依赖知识中心切片 3B 的部分——来源卡片→切片抽屉实时原文鉴权——按负责人「先做独立部分，需要依赖部分先不动」指示暂不实现，待 3B 落地后接入）。① Chat v1 API 客户端 `apps/web/src/api/chat.ts`：`openChatStream` 以 fetch 流式消费 `POST /api/v1/chat/stream` canonical SSE（携带 Bearer、增量解析逐事件回调、非 2xx 抛携带错误码的 `ChatStreamHttpError`），`cancelChatGeneration` 幂等调用 `POST /api/v1/chat/generations/{id}/cancel`（202）。② Conversation v1 API 客户端 `apps/web/src/api/conversations.ts`：列表（last_message_at 倒序分页）/详情（含消息历史）/创建（`knowledge_base_id` 对齐 Chat v1）/重命名 PATCH/删除 204，统一解包 `{code,message,data}` 信封，字段契约以 OpenAPI 为唯一权威源。③ canonical SSE 解析器 `chatSseParser.ts`：增量分帧解析 `meta → message.delta → sources → done`，心跳注释帧/未知事件（如 v1 不输出的 thinking）/畸形帧容错忽略，sources chunk 规范化填充 null 缺省字段。④ Chat SSE 状态机 `chatGenerationMachine.ts`（纯 reducer，FRONTEND §7）：Idle→Connecting→Streaming→AwaitingSources→Completed；只在 done 后进入成功终态；error 后拒绝拼接任何 delta；sources 与当前 generation 严格绑定、新一轮发送清空上一轮来源；用户中止进入 Canceling、连接关闭进入 Canceled（已中止）；SSE 意外断开（无 done 无 error）同样展示「已中止」不伪装完整答案。`useChatGeneration` hook 以 abort 归属校验丢弃陈旧流回调，避免取消竞态把新一轮 generation 误判为已中止；停止时先进入 canceling、中止连接并调用 cancel API 终止服务端生成。⑤ 据见问答页 `/chat`：知识库选择器（FRONTEND §5.5——单选、名称搜索、可见性/文档量辅助信息、多选入口展示「规划中」不可执行；切换知识库前具名确认会话范围变化）、消息列表、生成状态（生成中/已中止/失败）、停止按钮、done 后以服务端回读（会话详情）确认持久化、新会话写入 `?conversation=:id` 刷新可恢复；来源卡片渲染 sources 元数据与证据置信度，「联动知识切片与实时原文鉴权」待 3B 切片抽屉接入，本切片只渲染元数据不发起原文请求。⑥ 问答历史页 `/chat/history`：名称搜索、更新时间排序、重命名（Dialog）、删除具名二次确认（ConfirmDialog，FRONTEND §10）、打开进入 `/chat?conversation=:id`，删除成功后经服务端回读确认。路由 `/chat`、`/chat/history` 从占位页切换为真实页面。验证：RED 确认（解析器/状态机/API 客户端/两页面模块缺失致 5 组测试 collection/断言失败）；本机 vitest 新增 44 项全绿（解析器 8、状态机 15、conversation API 5、chat stream 4、问答页 6、历史页 6）、全量 83 项全绿；TypeScript `tsc --noEmit`、vite 生产构建、ESLint 零错误、Prettier 格式化通过。ADR 检查 1–8：否（纯前端消费已批准的 v1 外部 API/SSE 契约，不改变服务边界、权限模型、公共契约或数据生命周期；SSE 解析器与状态机是 FRONTEND §7 已指定的实现细节，属「局部、可替换、低成本回滚」不触发范围）。（2026-08-09）
- 落地 M4 切片 3B「知识中心」（对齐 FRONTEND §5.3/§5.4/§9/§10、UIDESIGN §6.1/§6.4/§6.6、docs/openapi/evidsight-v1.yaml，落实 ROADMAP M4 解除条件满足后的进行中状态）。① ROADMAP：M4 状态由「受阻」更新为「进行中」——M2 三条外部 API 退出门禁（commit `dc92313`，容器内 52 项契约/行为测试全绿）已满足解除条件，切片 3A（稳定 Segment ID，commit `8607c4c`）已落地，切片 3B 按排期推进；Provider 就绪清单 KB/Document/Conversation 从「按排期暂缓」更新为「消费中」。② 类型化 Knowledge API 客户端 `apps/web/src/api/knowledge.ts`：KB 统一可见列表（scope=all/mine/public + q 搜索 + 分页，对齐 OpenAPI ScopeParam/QParam/PageParam）、KB 创建/详情/更新/删除、Document 列表（status/filename/sort_by/order 参数）、上传（multipart file+force，202）、详情/删除/分块/重试/location 实时鉴权；响应统一解包 `{code,message,data}` 信封，`withAdapter` 供测试注入。③ 知识库列表页 `/knowledge-bases`：全部/我创建的/组织公开筛选、名称搜索、分页、private/public 文字+视觉双标记（StatusBadge，对齐 UIDESIGN §6.4 小型矩形）、新建/编辑（Drawer）、删除具名二次确认（ConfirmDialog，FRONTEND §10）；空态/错误态/加载 Skeleton。④ 知识库详情页 `/knowledge-bases/:kbId`：元数据、文档列表、六值状态映射（queued→等待处理/processing→处理中/completed→可检索/partial→部分可用/failed→处理失败/deleting→删除中）、上传抽屉（PDF/DOCX/MD/TXT 扩展名校验、force 覆盖、提交后可离开）、重试（partial/failed）、删除二次确认。⑤ 文档切片抽屉 `ChunkDrawer`：分块列表以稳定 `segment_id` 为唯一身份（不使用内部整数 `id`，对齐 API.md §6.2 迁移态）、展开时实时调用 `GET /api/v1/documents/{document_id}/locations/{location_id}` 鉴权；权限撤销（403/E5005）或来源不可用（E2015/404）时立即清除已显示正文并展示受限态，保留预览与元数据。验证：RED 确认（各模块缺失导致 3 组测试 collection/断言失败）；本机 vitest 新增 29 项全绿、切片 3B + 既有基线 39 项全绿、全量 82 项中唯一失败为并发切片 4 在途 `chat-page.test.tsx`（ChatPage done 后消息持久化未收敛，与本切片无关）；TypeScript 类型检查零错误、vite 生产构建通过、ESLint 零错误、Prettier 格式化、架构测试 21 通过/1 既有失败（`.opencode/package.json` 第二前端识别，并发 CI 条目已记录）。ADR 检查 1–8：否（纯前端实现，消费已批准的 v1 外部 API 契约，不改变服务边界、权限模型、公共契约或数据生命周期；后端未改动，仅按实际端点消费）。（2026-08-09）
- 落地 M2 收口：Knowledge v1 外部 API（KB/Document/Conversation）+ External OpenAPI + 契约测试（对齐 API.md §6—§7 目标态与 ROADMAP M2 三条退出门禁，负责人 2026-08-09 裁决「M2 退出前补齐 v1 Provider、External OpenAPI 与契约测试；M4 暂缓」）。① 新建 `docs/openapi/evidsight-v1.yaml`（OpenAPI 3.1.0，9 路径/17 操作）：KB 创建/统一可见列表/详情/更新/删除，Document 上传/列表/详情/分块/重试/删除/location，Conversation 创建/列表/详情/重命名/删除，含请求/响应/分页/查询/错误 Schema 与 `{"code","message","data"}` 信封，成为这些 DTO 字段的唯一权威源（API.md §3.1 改引用）。② 新增三个薄适配器 Provider：`knowledge_base_v1`（POST 201 / GET 统一可见列表 / GET 详情 / PATCH / DELETE 204）、`document_v1`（上传 202 multipart、列表、详情、分块含稳定 `segment_id`、retry 202 幂等、DELETE 204；文档级路径不再依赖 legacy 嵌套 `{kb_id}/documents/{doc_uuid}`）、`conversation_v1`（POST 201 请求统一 `knowledge_base_id` 对齐 Chat v1、列表、详情、PATCH、DELETE 204），复用既有 service 与 uuid 解析。③ 新增 `list_visible_kbs` service：`scope=all|mine|public` + `q` 名称搜索（LIKE 转义）、普通用户 all 为 mine ∪ public 且分页去重、admin all 治理可见、`updated_at DESC NULLS LAST`（MySQL 8.0 不支持 `NULLS LAST` 字面量，用 `IS NULL` 先行排序实现）、复用实时 chunk/doc 计数。④ 契约测试：KB/Document/Conversation v1 API 层 method/路径/状态码（201/200/202/204）/信封/错误码并断言响应 DTO 与 OpenAPI Schema 一致；`test_kb_list_visible.py` 在真实库（db_session）验证三类 scope、分页去重、搜索与 admin 治理可见；`test_openapi_consistency.py` 三方一致（FastAPI 路由清单、OpenAPI 路径清单与 Provider 契约测试覆盖表双向比对）。⑤ 修复测试基建：`db_session` fixture 此前未被任何测试使用，teardown 因 `asyncio_default_fixture_loop_scope=session` 与测试体 function 循环跨循环报「Task/Future attached to a different loop」，改为 `pytest_asyncio.fixture(loop_scope="function")` 并在 teardown dispose 引擎连接池。验证：RED 确认（v1 模块缺失导致 35 项失败，含 `module 'app.api' has no attribute 'knowledge_base_v1'` 与 OpenAPI 已声明但 FastAPI 未注册）；docker 容器内新增 52 项全绿、受影响 KB/document/conversation 单测 273 项全绿、contract 全量 93 项全绿、Knowledge 全量 unit 1497 项全绿；mypy 强制范围 6 个新增/改动文件零错误、ruff check/format 通过。ADR 检查 1–8：否（落实 API.md 已批准目标态，新增 v1 端点与可选查询参数均为向后兼容，不改变权限模型、服务边界、数据生命周期；迁移态裁决已记录于 API.md 2026-08-09 条目）。（2026-08-09）
- 落地基础 CI 骨架与低成本必过门禁（配置/开发工具变更，不修改业务行为）：TESTING 将自动化分为当前 PR 基础 CI、M5 部署/集成验收与 M6 冻结候选版本发布门禁；新增 `.github/workflows/ci.yml` 六个并行只读 job，覆盖 Python Ruff/全量 mypy、后端快速单元、Contract Schema/Fixture/Consumer 与隔离 Provider、Architecture/Compose 配置、External OpenAPI 语法/`$ref`/示例/路由/基线 Breaking Change，以及 Web lint/format/Vitest/type/build。新增三个本地复现脚本与 Make target；两服务生成带哈希的完整开发锁，Knowledge Dockerfile 增加不影响生产 `runtime` 的 `ci-test` stage。为同一锁覆盖 Intel macOS 与 CI Linux，将既有传递依赖约束为 `onnxruntime 1.23.x`、`cryptography 46.x`；根新增 `openapi-spec-validator`、`pytest`、`PyYAML` 开发依赖。Python/Pydantic 参考生成物仍无可重复生成命令，TypeScript 生成链亦未落地，不将缺席检查记为通过。验证：先以缺失 workflow 的 3 项 Architecture 验收正确观察 RED；锁定安装通过；快速单元 Knowledge **500 通过**、Research **434 通过**；Contract host Schema/Fixture **131 通过**，Research Consumer 与隔离 Provider 脚本整体退出码 0，Provider 容器 **49 通过**；OpenAPI 静态校验及受保护基线比较通过，路由/测试覆盖容器 **44 通过**；Web ESLint/Prettier、Vitest **8 文件/10 测试**、TypeScript/Vite build 通过；Python Ruff check/format 与 mypy Knowledge **211**、Research **240**、共享 **24** 个源文件零错误；`docker compose config --quiet`、哈希锁安装与 `uv lock --check` 通过。根 Architecture **24 通过/1 失败**，唯一失败为用户本地 `.opencode/package.json` 被既有第二前端检查识别，与本次变更无关，未删除或屏蔽。ADR 检查 1–8：否（自动化既有工程质量要求，不改变服务边界、公共契约、数据、安全或生产运行机制）。（2026-08-09）
- 完成 mypy 第六批并将 Python 门禁收口为全量范围：Knowledge/Research 完整 `app/`、两服务 `scripts/`、`tests/`、`alembic/env.py`，以及根 `tests/`、Contract Python 生成物与契约测试；Alembic 历史 revision 与 `.ab/` 实验目录按规范继续排除。首轮基线为 Knowledge **83 条/25 文件**、Research **153 条/37 文件**、共享范围 **4 条/4 文件**，清理 ORM、Evaluation、入口、脚本、测试替身、Optional 缩窄及字符串用户标识边界；静态检查发现 `verify_ac004_recovery_drill.py` 将已绑定的单任务函数错误传入两个参数，新增验收测试并亲自观察 `TypeError` RED 后修复。Knowledge 开发依赖新增 `types-PyYAML`/`types-jsonschema`（活跃维护的纯类型桩，仅进入 `.venv` 与 Docker `typecheck` target，生产镜像增量为 0；替代方案是对共享动态库局部放宽 `Any`，但会丢失 Contract 生成链校验）。统一脚本、pre-commit 和 Claude 只读 hook 已同步全量范围。验证：本机及 Linux Python 3.12 Docker mypy 均为 Knowledge **202**、Research **240**、共享 **22** 个源文件零错误；Research 全量 **1125 通过/1 跳过**，Contract+根架构测试在容器内屏蔽用户未跟踪 `.opencode` 目录后 **148 通过**；Knowledge 隔离容器 **1563 通过**，剩余 8 项均因该容器未连接 MySQL 而在 `localhost:3306` 拒绝连接，未将可能清表的测试接入正在运行的开发数据卷；uff/format、pre-commit 全量 mypy hook、`uv lock --check`、shell 语法与 `git diff --check` 通过。ADR 检查 1–8：否（扩大既有可逆静态质量门禁并修复一处脚本调用错误，不改变服务边界、公共契约、数据生命周期、安全机制或生产运行依赖）。（2026-08-09）
- 固化 Python 开发与类型检查环境（配置/开发工具变更，不修改业务行为）：新增唯一初始化入口 `make setup-python-dev`，以 `uv sync --locked --no-install-project` 建立根工具环境（避免根项目没有 `src/evidsight` 时错误尝试构建），并校验/同步 Knowledge、Research 各自 Python 3.12 `.venv` 的完整运行与开发依赖；mypy/pre-commit 后续只复用固定 venv，不在检查过程临时安装。两个服务 Dockerfile 新增可缓存 `typecheck` target，`make type-check-docker` 首次或 requirements 变化时构建依赖层，之后以只读挂载工作区运行 Linux Python 3.12 mypy；默认最后阶段仍为 `runtime`，开发依赖不进入生产镜像。同步 DEVELOPMENT/TESTING，明确本机 venv 仅用于编辑器、静态检查和纯单元检查，集成/Worker/迁移/跨服务/smoke 仍走 Docker Compose；Claude mypy hook 缺环境时明确提示初始化命令。验证：初始化连续执行两次均成功且第二次只检查既有包；本机 Knowledge **69**、Research **76** 个源文件零错误；Docker typecheck 首次构建及第二次缓存复跑均零错误，第二次所有依赖安装层均为 `CACHED`；单独构建两个 `runtime` target 后确认均无法发现 mypy 模块。ADR 检查 1–8：否（可逆开发工具链与构建 target 调整，不改变服务边界、公共契约、数据、安全或生产运行依赖）。（2026-08-09）
- 扩展 mypy 第五批强制范围至 Pipeline / Task / Worker：Knowledge 纳入 `app/rag/`、`app/ingest/`，Research 纳入 `app/pipeline/`、`app/agent/`、`app/tasks/`、`app/tools/`，并同步统一脚本、pre-commit 与 Claude Code 只读报告 hook；基础设施和外部 Provider 客户端留待最后一批。首轮基线为 Knowledge 69 个文件中的 15 个错误、Research 76 个文件中的 11 个错误，清理 PDF 解析资源状态、Reranker 属性、Evidence Graph 动态计数、内部候选类型、Agent 上下文生命周期及 Tool 无 Step 场景等类型边界。对无 `py.typed` 的 Celery、PyMuPDF、jieba、rank-bm25 仅添加按模块缺失类型声明豁免，不启用全局 `ignore_missing_imports`；Knowledge 开发依赖新增 `types-psutil`（仅开发/CI，生产镜像体积增量为 0）。验证：本机及 Linux Python 3.12 Docker 中 Knowledge 69 个源文件、Research 76 个源文件均零错误；Knowledge RAG/Ingest 单测 **671 通过**，Research Pipeline/Agent/Task/Tool 及受影响 Service 单测在注入测试占位 `TAVILY_API_KEY` 后 **421 通过**（首次未注入时 17 项均在既有 fail-fast 配置检查处失败，非业务回归）；ruff check/format check 通过。ADR 检查 1–8：否（扩大既有可逆静态质量门禁，不改变服务职责、公共契约、数据生命周期、安全机制或运行时依赖）。（2026-08-09）
- 补齐稳定 Segment ID 契约（切片 3A，知识中心前置阻断项）：分块列表 `GET /api/knowledge-bases/{kb_uuid}/documents/{doc_uuid}/chunks` 响应 items 新增 `segment_id`（映射 `chunks.segment_uuid`，即 v1 location 端点 `GET /api/v1/documents/{document_id}/locations/{location_id}` 的 `location_id`），保留旧 `id`（内部整数 PK）字段兼容；React 前端据此以稳定身份实时鉴权展开原文，不使用内部整数 `id`。Schema `DocumentChunkResponse` 增补必填 `segment_id`，`get_document_chunks` 填充 `chunk.segment_uuid`。新增验收：service 层 `segment_id` 映射与往返契约（chunk 列表暴露的 segment_id 可直接作为 location_id 解析正文）、API 层响应字段（segment_id 与旧 id 并存）；location 撤权/来源失效受限态由既有 E5005/E2015 测试覆盖。负责人于 2026-08-09 裁决「增补保留 id」。验证：RED 确认（3 项因 `segment_id` 缺失正确失败）；docker 容器内受影响 8 项全绿、document 相关全量 121 项全绿、Knowledge 全量 unit 1474 项全绿、mypy 强制范围 43 文件零错误。ADR 检查 1–8：否（新增向后兼容字段并保留旧字段，暴露 DATABASE.md §5.5 既有稳定 Segment ID，不改变权限模型/服务边界/数据生命周期；迁移态记录见 API.md §6.2 Chunk 列表迁移态）。（2026-08-09）
- 第四批范围同步到 Claude Code 的 mypy 只读报告 hook：编辑两服务 API、Middleware、Schema、Service、配置/依赖注入或已纳入 Core 文件后均会执行对应单文件检查，并与统一门禁一致使用 `--follow-imports=skip`；不修改文件。验证：脚本 `bash -n` 与 pre-commit mypy hook 通过。ADR 检查 1–8：否（同步既有静态门禁触发范围，不改变产品或运行行为）。（2026-08-08）
- 扩展 mypy 第四批强制范围至 Knowledge/Research 全部 `app/services/`，并纳入 Research 身份状态与 Internal Retrieval 两个跨服务客户端；基础设施/外部 Provider 客户端留待后续批次，pre-commit 触发范围与统一脚本同步。首轮基线为 Knowledge 43 个文件中的 10 个错误、Research 33 个文件中的 6 个错误，清理 Optional、SQLAlchemy 动态行、异构位置字典、异常缩窄、META 会话类型、报告通道集合与 DB 租约标识的类型边界。类型检查同时发现 Evidence 详情和关系接口在鉴权后记录并发消失时会返回 `200 data=null`；按 API.md 不存在/不可见 `404` 语义新增 2 个验收测试，正确观察 RED 后改为 `TaskNotFoundException`。验证：Python 3.12 Docker 中 Knowledge mypy 43 个源文件、Research 33 个源文件零错误；Knowledge 全部 Service 单测 **219 通过**；Research 全部 Service、两个跨服务客户端与 Evidence API **218 通过**。Knowledge 首轮完整验证另发现 Trace 聚合行改为元组解包后不兼容属性式测试替身（217 通过、2 失败），恢复属性读取并对动态计数显式转为 `int` 后全绿；ruff check/format check 通过。ADR 检查 1–8：否（扩大既有可逆静态质量门禁，并使并发删除失败语义重新符合现有 API 规范，不改变服务职责、跨服务调用方向、公共契约、数据生命周期或安全机制）。（2026-08-08）
- 引入 mypy 渐进式 Python 类型门禁（配置/开发工具变更，不修改生产行为）：TESTING §3.1 先定义首批强制范围为 Knowledge/Research `app/schemas/`、两服务 `app/core/permissions.py` 与 Research `app/core/utils.py`，明确零错误验收、分服务环境执行和禁止全局 `ignore_missing_imports`/`ignore_errors`/批量 ignore；根 `pyproject.toml` 启用 mypy 2.3、Pydantic plugin、`check_untyped_defs`、严格 Optional、冗余 cast 与无效 ignore 检查，不启用已废弃的 SQLAlchemy mypy plugin。两个服务新增独立 `requirements-dev.txt`，生产 Dockerfile 继续只安装 `requirements.txt`，运行时与生产镜像体积增量为 0；新增 `scripts/check_python_types.sh`、`make type-check`、全量测试入口和只读 pre-commit hook。首次基线无需修改生产代码或添加 ignore 即清零。验证：本机两个独立 venv 分别通过（Knowledge 9 个源文件、Research 4 个源文件）；对应 Python 3.12 Docker 镜像临时容器复核结果一致；`pre-commit validate-config` 与 mypy hook 通过，`uv lock --check`、shell 语法及 `git diff --check` 通过。ADR 检查 1–8：否（可逆、低成本的局部质量检查配置，不改变服务边界、公共契约、数据、安全或产品行为）。（2026-08-08）
- Agent 侧静态检查 hook 补齐为「ruff + mypy + 前端 ESLint/Prettier」三套（配置/开发工具变更，不修改生产行为；延续 2026-08-07 ruff「先报告、批准后再写」流程）。① `.claude/settings.json` PostToolUse 新增 `.claude/hooks/mypy-report.sh` 与 `.claude/hooks/web-lint-report.sh`（与既有 ruff-report.sh 并列，均只读报告不落盘）；`permissions.ask` 追加 `uvx ruff format*`/`uvx ruff check --fix*`（堵住此前 `uvx ` 前缀绕过裸 `ruff format*` 门禁的缺口）与前端写命令门禁 `pnpm --dir apps/web run format*`、`pnpm --dir apps/web exec eslint --fix*`、`pnpm --dir apps/web exec prettier --write*`、`npx eslint --fix*`、`npx prettier --write*`。② 新增 `.claude/hooks/web-lint-report.sh`：Edit/Write `apps/web` 源码后以 `eslint --fix-dry-run`（只读）+ `prettier` 与当前文件 diff 报告改动预览，批准方式对应 `pnpm --dir apps/web run format` / `eslint --fix`。③ 新增 `.claude/hooks/mypy-report.sh`：范围镜像 `.pre-commit-config.yaml` 的 python-mypy 首批范围（两服务 `app/schemas/*` + 指定 `app/core/*.py` 模块），运行对应服务 venv mypy 只读报告类型错误，干净静默；mypy 无写命令故不设权限门禁。④ `ruff-report.sh` 批准提示同步 `uvx ruff format` 形态。验证：`settings.json` JSON 合法、三脚本 `bash -n` 通过、模拟 PostToolUse 输入冒烟——web hook 对不合 prettier 临时文件输出 diff 且文件 MD5 不变、非前端/干净文件静默；mypy hook 对范围内干净文件静默、对注入类型错误的临时 schemas 文件输出 `Found 1 error`、范围外文件静默；pre-commit 前端 hook 已存在（`web-eslint`/`web-prettier`）本次未改动。ADR 检查 1–8：否（可逆、低成本的局部工具链配置，不改变服务边界、公共契约、数据、安全或产品行为）。（2026-08-08）
- 补齐 M4 Web 工程工具链规范并完成 npm → pnpm 迁移：ROADMAP 将 M4 状态校正为「进行中」，FRONTEND §2.2 作为 pnpm、ESLint、Prettier、`.editorconfig` 与 pre-commit 前端门禁的唯一权威来源，TESTING 仅投影验收条件；`apps/web/package.json#packageManager` 固定 pnpm 11.18.0，删除 `package-lock.json` 并生成 `pnpm-lock.yaml`，`pnpm-workspace.yaml` 仅放行构建必需的 esbuild/MSW 生命周期脚本。新增 ESLint 9 + `typescript-eslint` + React Hooks/Refresh 插件、Prettier 3 与根 `.editorconfig`；均为活跃维护的开发依赖，浏览器生产 bundle 增量为 0，pnpm 共享 store 下本地安装体积随复用情况变化，替代方案与边界见 DEVELOPMENT §9。现有 pre-commit 增加 Web lint/format 只读 hook，README、Makefile、测试/冒烟脚本与当前开发指南统一改用 pnpm；M0 迁移计划中的历史 npm 取证命令保持不变。为维持 Node 20+ 基线，ESLint 保持 9.x，并将已被错误发布为破坏性版本的 `@testing-library/jest-dom` 固定为 6.9.1。配置变更按 AGENTS.md 例外不制造业务 RED。验证：Node 22 Docker 容器内 `pnpm install --frozen-lockfile`、ESLint（零 warning）、Prettier check、Vitest **8 文件/10 测试全绿**、`tsc --noEmit && vite build` 全部通过；`pre-commit validate-config`、前端两个 hook、`git diff --check` 与 `docker compose config --quiet` 通过。ADR 检查 1–8：否（可逆、低成本的局部开发工具链调整，不改变服务边界、公共契约、数据、安全或产品行为）。（2026-08-08）
- 补齐 Chat v1 canonical SSE、generation 生命周期与旧入口兼容观测（对齐 API.md §7/§12、DATABASE.md §6.3，负责人 2026-08-08 裁决“实现服从规范”并批准取消状态机）。新增 `POST /api/v1/chat/stream`，将旧流投影为 `meta → message.delta → sources → done` canonical 事件并为每个事件生成单调 ID；新增 `POST /api/v1/chat/generations/{generation_id}/cancel`，仅 `pending|running` 可取消，重复取消 `canceled` 返回 202 且 `idempotent_replayed=true`，`completed|failed` 返回 `409 CHAT_GENERATION_STATE_CONFLICT`，不存在与非创建者统一返回安全 `404 CHAT_GENERATION_NOT_FOUND`。新增 `chat_generations` 生命周期事实表及 `messages.generation_id` 关联迁移；正常完成、失败、显式取消与 SSE 断开通过行锁只形成一个终态，失败或取消不持久化伪成功 Assistant Message。旧 `/api/chat` 保留原事件语义并以 Redis 命名空间计数器记录按路由区分的废弃调用量。验证：正确 RED 后进入 GREEN；Docker Compose Knowledge 受影响集合 86 项全绿；生命周期与旧服务回归 45 项全绿；真实 `knowledge_db` Alembic 从 `c6d7e8f9a0b1` 升级到 `c8d9e0f1a2b3` 成功。ADR 检查 1–8：否（落实既有目标契约和生命周期规范，不改变服务边界、权限或数据生命周期）。（2026-08-08）
- 落地 Web 身份认证与登录切片（对齐 FRONTEND §4.2/§5.1、API.md §5、IDENTITY_AND_ACCESS §4.2）。新增 React 类型化 Auth/API 客户端：Access Token 仅保存在内存，Refresh/Logout 读取 `evidsight_csrf` Cookie 并回传 `X-CSRF-Token`，不读取、存储或发送 Refresh Token 明文；并发 `401 E5003` 共享单次刷新并重放请求，刷新失败清理身份及注册的 Query/SSE/敏感状态。登录与刷新后必须调用 `/api/v1/auth/me` 建立当前用户事实，恢复完成前受保护路由不放行；新增入口叙事页上的登录/注册抽屉、提交防重复、显示密码、原目标返回与主题偏好持久化。验证：Docker Node 22 容器内切片身份测试 7 项全绿；累计 React 测试 8 项全绿；TypeScript `tsc --noEmit` 与 Vite 生产构建通过。ADR 检查 1–8：否（实现已接受 ADR-005/ADR-006 与现有前端规范，不新增身份机制或持久化边界）。（2026-08-08）
- 落地统一 Web 应用壳层与工作台切片（对齐 FRONTEND §3.2–§5.2、UIDESIGN §5）。新增 232px 普通业务壳层、56px 上下文栏及固定顺序六项一级导航（工作台、据见问答、问答历史、知识库、深度研究、研究任务）；账号与管理入口固定底部，管理入口仅按 `/me` 角色展示且服务端继续承担最终鉴权；顶部运行任务提示只导航到任务列表，不表达 SSE 状态。工作台提供快速提问/深度研究入口、RECENT RESEARCH/RECENT KNOWLEDGE 结构与“开始研究/创建知识库”真实空态，不伪造 KPI；新增可复用 Skeleton、EmptyState、ErrorState。验证：Docker Node 22 容器内壳层与工作台 2 项验收测试全绿；累计 React 测试 10 项全绿；TypeScript 与 Vite 生产构建通过。ADR 检查 1–8：否（落实既有导航、状态和视觉规范，不改变公共契约或权限模型）。（2026-08-08）
- 接受 `ADR-011`，确认三台 2C2G 云服务器按 Edge/Research、Data、Knowledge 数据岛拆分的 v1.0 生产拓扑，并明确 Mac 保留单机全栈开发但不进入生产依赖、Windows 只承担非关键辅助职责；负责人于 2026-08-08 明确裁决「接受 ADR-011」，解除总体架构、运维、配置和测试规范更新门禁。（2026-08-08）
- 落地 M4 进入条件第 1 项「M2、M3 的外部 API、SSE 和 Evidence Contract 已稳定」的缺口——API.md §9 Evidence 与 Report 读取端点及其目标态数据层（走目标态方案 A1，用户 2026-08-07 裁决；落实已 accepted 的 ADR-009 与 DATABASE.md §7/API.md §9，不改变服务边界/安全/公共契约语义）。① 数据层：`evidence_items`/`report_sections` 新增对外 `external_id`（UUID，存量 MySQL `UUID()` 回填）；新建 `reports`（task_id 唯一、`current_revision_id` 延迟 FK）、`report_revisions`（`(report_id, revision_number)` 唯一、status building|published|failed、完整度摘要、`based_on_revision_id` 谱系）、`claims`（revision/section FK、sequence、certainty high|medium|low、qualification）、`evidence_relations`（`(claim_id, evidence_id, relation_type)` 唯一、confidence 0-1、`created_by_step_id`）；迁移 `g4h5i6j7k8l`、`g5h6i7j8k9m`（report_sections 加 revision_id）经真实 MySQL upgrade/downgrade/再 upgrade 往返验证通过。② Synthesis 扩展（RESEARCH_PIPELINE §8.1/§9.5）：输出 `claims[]`（statement/critical/certainty/qualification/evidence_relations[{evidence_index,relation_type,confidence}]），程序化校验 statement 非空、certainty 受控枚举、relation_type 受控枚举、confidence 0-1、evidence_index 合法（越界过滤）；含 contradicts 关系不得合成为无条件确定结论。③ Evidence Graph Build：从 Synthesis claims 关联 Graph items 输出 `graph["claims"]`（越界/非整数索引过滤、非数组不阻断）。④ Renderer 目标态原子发布（DATABASE.md §7.6）：`report_publisher.publish_report` 创建 reports→building Revision→sections/claims/relations→计算完整度三分项→置 published 并切换 `current_revision_id`；**方案 X**（负责人 2026-08-07 裁决）：revision 发布时为 sections 创建独立副本，不挂载/修改迁移态 task 级 sections，reports/revision 级联删除只影响 revision 副本、不破坏迁移态展示数据；新增完整度纯函数模块 `app/evaluation/completeness.py`（question_coverage/channel_success/claim_coverage 加权、含 rule_version）。⑤ 读取 API（API.md §9，task READ）：`GET /api/v1/research/tasks/{task_id}/evidence`、`GET /api/v1/evidence/{evidence_id}`（对外 UUID）、`GET /api/v1/evidence/{evidence_id}/relations`、`GET /api/v1/reports/{report_id}`、`GET /api/v1/reports/{report_id}/sections/{section_id}`，内部 Evidence 不返回正文（ADR-003）、web 返回 URL/获取时间快照；实现于 `app/api/research_evidence.py` + `app/services/report_reader.py`，main 挂载 `/api/v1`。验证：切片 RED 逐项确认；research 全量 unit（docker 容器，非 slow）963 通过、1 跳过、0 失败（排除 `test_llm` 环境依赖）、契约 73 项全绿、Architecture 16 项全绿（1 项既有前端布局失败，stash 前后一致非本次引入）、新增测试 66 项（模型 24、synthesis claims 10、graph claims 4、publish 6+9、API 7、完整度 6）；真实 research_db 端到端冒烟：存量 10 条 evidence external_id 回填可读、目标态发布+读取 API 返回 published revision 1/5 sections/完整度摘要、AC-001 100%/AC-010 100% 兼容通过。**验证事故记录**：冒烟验证发布测试报告后 `DELETE FROM reports` 清理，因原实现把历史迁移态 sections 挂载到 revision，级联删除了历史冒烟任务（`53d881ec`，2026-08-02）的 5 条 sections 与 31 条 section_evidence（evidence/steps/task 完好）；根因已按方案 X 修复（副本语义），已删历史展示数据无法自动恢复，任务数据为冒烟级非生产。ADR 检查 1–8：否（实现已接受的 ADR-009 与 DATABASE.md §7/API.md §9 目标态，负责人 2026-08-07 裁决走目标态方案，不改变服务边界/权限模型/公共契约语义；记录见本条目）。（2026-08-07）
- 落地「内部 Evidence 原文访问实时权限复核接口」`GET /api/v1/documents/{document_id}/locations/{location_id}`（对齐 API.md §6.2 / IDENTITY_AND_ACCESS §9 / ADR-003；落实 ROADMAP M4 进入条件「内部 Evidence 原文访问的实时权限复核接口可用」）。`location_id` 语义经负责人确认 = Segment 稳定 UUID（`chunks.segment_uuid`）。实现：Knowledge 新增 `v1_router`（prefix `/api/v1/documents`，main 挂载，nginx `/api/` fallthrough 已可达）；`document_service.get_document_location` 复用 `check_kb_active` + `require_kb_readable` 实时鉴权（用户状态由 `get_current_user` 复核），再按文档 Active Version 精确解析 Segment 稳定身份——文档缺失 `E2001`、KB 不存在/非 active `E1001`、无 READ 权限 `E5005`、segment 非 Active Version/无内容/无安全位置统一 `E2015 EvidenceSourceUnavailableException`「来源不可用」（IDENTITY_AND_ACCESS §9「明确受限/不可用状态」），`minimal_excerpt` 仅返回当前可读片段不持久化；`_derive_location` 优先页码、其次章节路径、均缺不伪造（对齐 Contract `SourceLocation`）。新增 `DocumentLocationResponse` schema；API.md §6.2 补充 location_id 语义、信封与 E2015 说明。验证：RED 确认（`EvidenceSourceUnavailableException` 缺失 + 路由缺失导致两测试模块收集失败）；docker 容器内新增 service 13 项 + API 5 项 + 文档相关回归 142 项全绿、Knowledge 全量 unit 1473 项全绿、contract 49 项全绿、ruff check/format 通过、容器内路由注册与 401 鉴权冒烟通过。ADR 检查 1–8：否（让实现重新符合既有规范 API.md §6.2 的接口补齐，不改变服务边界/权限模型/公共契约/数据生命周期）。（2026-08-07）
- Agent 侧 ruff「先报告、批准后再写」流程（仅 Python 文件；与 TESTING.md「lint 只报告」约定一致，报告只读、写入需显式批准）：opencode 通过 `.opencode/plugin/ruff-report.ts`（`tool.execute.after` hook，编辑/写入 `.py` 后自动输出 `ruff format --check --diff` 与 `ruff check --diff` 预览到工具结果）+ `opencode.json` 权限（`ruff format*`/`ruff check --fix*` 设为 `ask`，执行写命令前弹窗确认）；Claude Code 通过 `.claude/settings.json`（`PostToolUse` 匹配 Edit/Write → `.claude/hooks/ruff-report.sh` 只读报告 diff，不落盘；`permissions.ask` 拦截 `ruff format*`/`ruff check --fix*`）。plugin 类型经根 `tsconfig.json` + `.opencode/plugin/opencode-plugin.d.ts` 本地最小声明解析（覆盖 `$`/`worktree`/`tool.execute.after` 签名），不引入 node 依赖、不新建 package.json，跨机器可移植；`ruff-report.ts` 以 `import type` 相对引用该声明（运行时被 transpile 擦除，opencode 自带 bun 加载不受影响）。验证：模拟 PostToolUse 输入，格式问题文件输出 diff 预览且文件未被修改、合规文件静默无输出、两个 JSON 配置语法合法；`tsc --noEmit` 对 plugin 类型检查通过（`$`/hook 签名/`input.args.file_path` 类型正确）。ADR 检查 1–8：否（工具链配置与本地 hook，不改变服务边界、公共契约、数据与安全或核心机制；属 AGENTS.md 例外条款）。（2026-08-07）
- 引入 pre-commit 提交前静态门禁（仅 Python 侧，前端不动；只报告、不修改文件）：按「先文档后实现」登记根 `pyproject.toml` 开发依赖（用途/维护状态/体积/替代方案见 DEVELOPMENT.md §9），新增 `.pre-commit-config.yaml` 三个 `repo: local` hook——`ruff check`、`ruff format --check`（以 venv 内 ruff 执行，版本随根 uv.lock 不重复固定，只对暂存文件运行）与 `commit-msg` hook（`scripts/check_commit_msg.sh` 强制 `add|fixed|update|refactor: 中文描述`，放行 merge/revert/fixup!/squash!）。存量 ruff 基线（611 告警 / 355 文件未格式化）按「触碰即清理」增量消解，不做全量预清理；pre-commit 不对暂存文件以外的代码生效，首个触碰旧文件的提交被阻断属预期。同步 DEVELOPMENT.md §3 命令（每个 worktree 初始化时执行 `pre-commit install` 两次）与 §9 编码约定、TESTING.md §3.1 静态边界与 §6 基线验证命令。验证（临时 git 仓库端到端）：违规文件提交被 `ruff check` 阻断、非法提交信息被 `commit-msg` 阻断、合规文件与合法信息提交通过，`docker compose config --quiet` 不受影响；`pre-commit validate-config` 通过。说明：根 `uv sync`/`uv add` 存在既有构建失败（`src/evidsight/__init__.py` 已被清理，uv_build 期望该模块），本次以 `uv add --dev pre-commit --no-sync` 更新锁文件 + `uv pip install` 装入 venv，不影响本变更有效性；该根骨架问题另议。ADR 检查 1–8：否（可逆、低成本、局部的工具链配置变更，不改变服务边界、公共契约、数据与安全或核心机制；属 AGENTS.md 例外条款，已登记依赖并执行格式/配置验证）。（2026-08-07）
- 引入 Python lint/格式化工具 ruff（仅 Python 侧，前端不动）：按「先文档后实现」登记根 `pyproject.toml` 开发依赖（用途/维护状态/体积/替代方案见 DEVELOPMENT.md §9），配置 `[tool.ruff]`（规则集 `E4,E7,E9,F,I`，行宽 100，排除 alembic 迁移与评估 fixture 等长行资产），同步 DEVELOPMENT.md §3 命令与 §9 编码约定、TESTING.md §3.1 静态边界与 §6 基线验证命令。引入阶段不批量修改既有代码：ruff 只报告（`ruff check`/`ruff format --check`），基线告警待后续分批清理，避免干扰 M3 进行中工作。验证：`uvx ruff check services/ scripts/ tests/ packages/contracts/` 报告 611 个告警（可自动修复为主：I001 导入排序、F401 未使用导入等）且退出码非零、`uvx ruff format --check` 报告 355 文件待格式化，均为预期基线而非回归；`uvx ruff --version` 输出 0.16.1。ADR 检查 1–8：否（可逆、低成本、局部的工具链配置变更，不改变服务边界、公共契约、数据与安全或核心机制；属 AGENTS.md 例外条款，已登记依赖并执行格式/配置验证）。（2026-08-07）后续一次性全量清理（超「触碰即清理」范围，负责人确认）：`ruff check --fix` 修复 470 处、剩余 87 处不可自动修复（E402 模块级导入未置顶 45、F841 未使用变量等）；发现并修复 1 处 ruff F401 误删导致的回归——`services/research/app/metrics/registry.py` 被删的 `CONTENT_TYPE_LATEST` 实为 `main.py`（media_type）经 `metrics/__init__.py` re-export 消费的导出链成员，以 `CONTENT_TYPE_LATEST as CONTENT_TYPE_LATEST` 显式 re-export 语法恢复。验证（docker 全栈容器挂载工作区）：research 全量（非 slow）1016 passed / 14 failed（13 项 integration 鉴权 403 + `test_llm` 模型环境依赖，与 clean HEAD 失败集完全一致）、knowledge 1536 passed、Architecture 17 passed、packages/contracts 131 passed，确认无回归。（2026-08-07）存量 87 处不可自动修复告警清零（提交前门禁验证时发现，逐项人工修复）：E711 5 处（SQLAlchemy `col == None` → `col.is_(None)`，research_service/pipeline/evidence_graph/synthesizer 及测试）；F841 34 处——测试类 31 处删除未使用 `as mock_xxx` 绑定/未用变量（patch 上下文管理器不受影响）、生产代码 3 处保留副作用调用仅去赋值（`document_service.py` 的 `_get_doc_in_kb` 鉴权、`knowledge_pipeline.py` 冗余 `coarse_input_count`、`pipeline_orchestrator.py` 未用 `execution_context`/`recoverable`），`knowledge/app/main.py` 3 处删除纯 getter `get_request_id()` 冗余赋值并移除对应导入；E402 42 处——`research/api/research.py` 与 `main.py` 将 logger/延迟路由导入移回顶部（无循环依赖）、`celery_app.py` 任务模块注册保留延迟导入加 `# noqa: E402`、eval/迁移/回归入口脚本的 `sys.path` 自举导入为刻意模式，在 `pyproject.toml` 新增 `[tool.ruff.lint.per-file-ignores]`（`migrate_to_per_kb_collections.py`、`tests/eval/*.py`、`tests/regression/*.py` 忽略 E402）；全仓 `--all-files` 扫描补修 `metrics/__init__.py` 三个 re-export 函数补入 `__all__`、`evaluation/manual.py` logger 移位、`test_types.py` 末端 `timedelta` 导入提前至顶部。`.pre-commit-config.yaml` ruff 两个 hook 新增 `exclude: alembic/versions`（pre-commit 传绝对路径使 pyproject 的 `services/*/alembic/versions/*.py` exclude 不生效，hook 侧显式排除保持一致）。验证：`ruff check` 与 `ruff format --check` 全仓清零、`pre-commit run --all-files` 三项 hook 全 Pass、commit-msg 正/反例验证通过；回归（docker 全栈容器挂载工作区）：research 1016 passed / 14 failed 与基线一致、knowledge 1536 passed、Architecture + contracts 148 passed，无回归。（2026-08-07）
- 落地 M3 切片 H「AC 验证入口」（对齐 TESTING.md §5/§7、PRD §12，落实 ROADMAP M3 退出门禁「PRD AC-001、AC-003、AC-004 和 AC-010 对应验证入口已建立」）。① 共享纯函数 `app/evaluation/ac_metrics.py`——`evaluate_citation_validity`（AC-001：报告章节 [来源N] 引用闭合到该章节实际关联且存在于 Evidence Graph 的 Evidence）、`compute_task_success_rate`（AC-003：completed+partially_completed 为成功、canceled 排除分母）、`compute_recovery_success_rate`（AC-004：演练样本成功率）、`check_evidence_traceability`（AC-010：source_type 合法；internal 需 KB/Doc/Version/Segment 稳定 ID + location_summary；web 需 canonical_url_snapshot + fetched_at_snapshot）。② 四个验证脚本（均输出 TESTING.md §7 发布记录模板、缺失数据报错退出不伪造）——`scripts/verify_ac001_claim_evidence.py`（--task-id/--all-completed，逐章节解析正文 [来源N] 并校验闭合，门槛 ≥90%）、`scripts/verify_ac003_task_success.py`（--tasks 冻结评估集，按 task_id 或 topic 解析终态统计，门槛 ≥95%）、`scripts/verify_ac004_recovery_drill.py`（--tasks/--samples，对 running 任务过期租约→运行 recover_stale_tasks→校验 recovery_count 递增，门槛 ≥95%）、`scripts/verify_ac010_traceability.py`（--task-id/--all-completed，逐条校验 Evidence 可追溯，门槛 100%）。③ 冻结评估集 `tests/eval/research_eval_set.json`（6 题，覆盖 comparison/explainer/analysis，AC-003/004 输入）。④ 修复 AC-010 缺口（用户确认）：Rerank web Evidence 持久化从 `research_sources.fetched_at` 穿透写入 `fetched_at_snapshot`（DATABASE.md §6.2；FetchedDoc→Candidate→Evidence 全链携带，internal 不写该列）；AC-010 脚本对迁移态 web 证据（snapshot 列为空）按 research_sources 回填 URL/获取时间判定，使存量与新建证据均按实际来源可追溯。验证：RED 确认（ac_metrics 模块缺失 + fetched_at_snapshot 为 NULL 导致用例失败）；本地全量 unit（非 slow，source 根 .env）929 通过、1 跳过、1 failed（`test_llm` 为本机根 `.env` 覆盖 `LLM_MODEL` 的环境依赖，与本切片无关）、契约 73 项 + `packages/contracts/tests` 131 项 + Architecture 17 项全绿；integration/acceptance 5 项失败与 clean HEAD 失败集一致（既有环境依赖）；docker research-api 容器内对真实 research_db 冒烟验证：AC-001 100%（1 任务 42 引用全闭合）、AC-010 100%（10 条 web 证据经 research_sources 回填可追溯）通过，AC-003/AC-004 对不存在任务 fail-closed 报「未通过」不伪造，AC-003 对真实 completed 任务 100% 通过；新增 `test_ac_metrics` 23 项、`test_rerank_fetched_at` 1 项。ADR 检查 1–8：否（纯 Research 本地只读验证脚本 + 既有列 fetched_at_snapshot 的写入语义补齐，不改变 API/SSE/Contract、权限、数据生命周期或服务边界）。（2026-08-07）
- 补齐 M3 退出门禁「冲突来源不会被合成为无条件确定结论」直接测试证据（对齐 RESEARCH_PIPELINE §9.5 / PRD FR-EV-003，此前仅有冲突结构透传的间接证据）：新增 `tests/unit/pipeline/test_synthesis_conflict_consensus.py` 5 项，`_parse_synthesis_output` 程序化拒绝「含非空 conflicting_evidence_indices 的 cluster 标记 consensus_level=strong」（存在 contradicts 不得合成为无条件确定结论；冲突 cluster 允许 moderate/weak 降级表达，无冲突 cluster 仍允许 strong 真共识）。验证：RED 确认（2 项 strong 拒绝用例失败）；补充后 research 全量 unit（非 slow，source 根 .env）1024 通过、1 跳过、6 failed 与 clean HEAD 失败集一致（integration 5 项 + test_llm 1 项环境依赖，与本切片无关）；新增 5 项全绿且既有 synthesizer/evidence_graph/renderer 70 项无回归。ADR 检查 1–8：否（让实现重新符合既有规范 PRD FR-EV-003 / RESEARCH_PIPELINE §9.5 的校验补齐，不改变服务边界、公共契约、数据生命周期或权限）。（2026-08-07）
- 落地 M3 切片 G「预算冻结/预留/结算 + 预算停止终态 + 报告披露」（对齐 RESEARCH_PIPELINE §14、DATABASE.md §5.1/§3.2、CONFIGURATION.md §3.1，落实 §17.1/§17.2 相关验收）。方案：**服务端默认推导**（负责人确认，零公共契约变更）——客户端不传预算字段，服务端按 `max_sources`/来源策略/服务端常量推导冻结上限并存入 Task。① 数据层——`research_tasks` 新增 `budget_frozen`（冻结上限快照，含 `schema_version`）、`budget_usage`（结算用量快照，含 `schema_version`）、`budget_stopped_at`（预算停止时间）三列（迁移 `f3g4h5i6j7k`，down_revision=e2f3a4b5c6d，真实 MySQL upgrade/downgrade/再 upgrade 往返验证通过，`SHOW COLUMNS` 确认三列存在）；DATABASE.md §5.1 补充「预算」字段组。② 预算服务 `app/services/budget_service.py`——`derive_default_budget` 按来源策略推导冻结上限（`knowledge` 无 Web Fetch，`max_fetch=0`，对齐 §17.1「knowledge 的 Fetch 明确为 skipped」；`web`/`hybrid` 取 `FETCH_MAX_URLS_PER_TASK`；`max_search_results` 取 `TAVILY_TOTAL_RESULTS_LIMIT` 与 `max_sources` 较大值）；`freeze_budget` 创建任务时写入冻结快照并初始化零用量；`can_reserve`（§14「每次外部调用前预留」：预算已停止或超过总时限则拒绝新调用）；`settle_budget`（§14「完成后结算实际用量」：累加 provider_calls/llm_tokens/cost_usd/sub_questions/search_results/fetch，任一维度用量 ≥ 上限即标记 `budget_stopped_at` 并返回停止标记）；`budget_stop_reason`/`budget_disclosure`（报告披露文案）。③ AgentRuntime 接入——`_execute_tool` 执行外部调用前 `can_reserve`，无法预留抛 `BudgetExhaustedError`（§14「无法预留则停止新调用」）；Tool 完成后 `_settle_tool_budget` 结算实际用量（LLM token 经 `extract_step_cost` 提取，search/fetch 成本与计数维度从 output 提取）；触发停止时 `_record_budget_stop` 记录 `budget.stop` agent_event（白名单已有）并发布 `task.warning` SSE；`run()` 捕获 `BudgetExhaustedError` 后不直接写 failed，而是经 `_finalize_task` 由 Resolver 推导终态。④ Resolver 预算停止终态——§14「预算停止不是自动成功：已有 Evidence 仍需通过完整度硬门槛；达标可 partial，否则 failed」：`TaskStateResolver.resolve` 在 FATAL 检查后检测 `budget_stopped_at`，走新增 `_evaluate_budget_stop`（复用 `min_evidence = max(5, ceil(max_sources*0.4))`，达标 → `partially_completed`，否则 → `failed` E3103「来源量不满足最小阈值」）。⑤ 报告披露——`renderer.run_render` 在预算停止时把 `budget_disclosure` 注入 `knowledge_gaps`，使报告生成必须披露因预算导致的缺失（§14「报告必须披露因预算导致的缺失」）。⑥ 配置——新增 `BUDGET_MAX_SUB_QUESTIONS`（5）/`BUDGET_MAX_LLM_TOKENS`（100000）/`BUDGET_MAX_PROVIDER_CALLS`（60）/`BUDGET_MAX_COST_USD`（1.0）/`BUDGET_DEADLINE_SECONDS`（3600），登记 CONFIGURATION.md §3.1 服务内部键。验证：RED 确认（`budget_service` 模块缺失 + Resolver 预算停止分支缺失导致 25 项新增用例 collection/断言失败）；docker 容器内 research 全量 unit（非 slow）905 通过、1 跳过、1 failed（`test_llm` 为本机根 `.env` 覆盖 `LLM_MODEL` 的环境依赖，与切片 E/F 同项）、AgentRuntime 集成 + 契约 75 项全绿、`packages/contracts/tests` 131 项全绿、Architecture 17 项全绿；本地 unit 890 通过、1 跳过、16 项失败与 clean HEAD 失败集完全一致（均为缺失 TAVILY_API_KEY/Redis 的既有环境依赖，非本切片回归）；迁移 MySQL upgrade→downgrade→upgrade 往返验证通过且 `SHOW COLUMNS` 确认三列；新增 `test_budget_service` 15 项（冻结推导/来源策略/knowledge 无 Fetch/预留结算/各维度超限停止/未超限不停止/总时限）、`test_task_state_resolver_budget` 5 项（预算停止证据达标→partial/不足→failed E3103/边界刚好达标/零证据 failed/未预算停止仍走 running 守卫）、`test_runtime_budget` 3 项（Tool 执行预留后结算/预算用尽拒绝新调用/budget.stop 事件）、`test_report_budget_disclosure` 3 项（披露文案/未停止无披露/渲染提示含披露）；既有测试适配（`test_task_state_resolver`、`test_pipeline_orchestrator` 的 `_make_task` fixture 显式置 `budget_stopped_at=None`，避免裸 MagicMock 自动创建 truthy 属性误触预算分支）。ADR 检查 1–8：否（服务端默认推导方案经负责人确认；实现已确认的 RESEARCH_PIPELINE §14 与 DATABASE.md §5.1 语义，不改变服务边界/公共契约/安全/数据生命周期，新增列为可回滚的向后兼容字段扩展）。（2026-08-06）
- 落地 M3 切片 F「SSE 持久游标 + agent_events 表」（对齐 RESEARCH_PIPELINE §15/§16、DATABASE.md §5.4/§9/§11、API.md §13，落实 §17.3 第 11/22 项验收）。① 数据层——新增 `agent_events` 追加式业务执行审计表（迁移 `e2f3a4b5c6d`，down_revision=d1e2f3a4b5c）：`id`/`task_id`(FK CASCADE)/`step_id`(FK SET NULL)/`sequence`/`event_type`/`tool_name`/`provider_name`/`input_summary`/`result_summary`/`request_id`/`trace_id`/`duration_ms`/`cost_summary`/`created_at`，`(task_id, sequence)` 唯一作为 SSE 持久游标（§9），事件只含安全业务摘要；真实 MySQL upgrade/downgrade 往返验证（downgrade 删表、再 upgrade 恢复，head=e2f3a4b5c6d）。② Agent Event Service——`event_type` 白名单枚举（`phase.enter`/`tool.request`/`tool.result`/`retry`/`budget.stop`/`recovery`），`append_event` 单调 sequence（MAX+1 + 唯一约束），`list_events_after` 按游标回放，且对 input/result 摘要递归剥离禁止字段（thought/reasoning/reasoning_content/prompt/excerpt/content/密码/token/cookie/stack 等，防御性落实 §16 与 §17.3-22）。③ AgentEventRecorder 接入 AgentRuntime——`record()` 先追加 agent_event 到 DB（与 Step 同事务随业务提交持久化），再把持久 sequence 作为 SSE `event id` 发布（客户端收到的事件在重连时可回放）：`_start_step` 阶段进入落 `phase.enter` 并发布 `phase.started`，AgentLoop Tool 请求/结果落 `tool.request`/`tool.result` 并发布 `agent.action`/`agent.observation`（载荷沿用既有脱敏参数/observation）。④ SSE 投影修正——移除 `agent.thought` 发布（原把模型 `reasoning_content` 发到用户可见字段，违反 §16/§17.3-22；ReAct thought 仅保留在内部 `agent_memory_entries` 供断点续跑，不落 agent_events、不外发）；DB 事实类事件（step.started/step.completed/task.progress/checkpoint/终态）不带持久游标、不携带 `id:`，由快照收敛。⑤ SSE 持久游标——`sse_event_stream` 新增 `last_event_id` 与 `replay_loader`：连接先推快照，再回放持久游标之后的 agent_events（`_agent_event_to_sse` 把事件类型投影回 `phase.started`/`agent.action`/`agent.observation`），随后订阅 Redis live；事件缺口以新快照收敛，不依赖 Redis 历史恢复业务事实（§15）；API `GET /api/research/{task_id}/stream` 读取 `Last-Event-ID` 头并注入回放加载器，无游标时回放全部已持久化事件（新订阅收敛），终态任务仍只推快照。验证：RED 确认（模块缺失 + 游标回放缺失导致 15 项新增用例 collection 失败）；本地全量 unit（非 slow，source 根 .env）882 通过、1 跳过、1 failed（`test_llm` 为本机根 `.env` 覆盖 `LLM_MODEL` 的环境依赖，与本切片无关）、AgentRuntime 集成 2 项通过、契约 73 项 + `packages/contracts/tests` 131 项全绿、Architecture 17 项全绿、迁移 MySQL 往返验证通过；新增 `test_agent_event_service` 8 项（sequence 单调/跨任务独立/非法类型拒绝/摘要剥离禁止字段/游标回放/末尾空/跨任务隔离）、`test_agent_event_recorder` 3 项（落库并带 sequence 发布/不发布也不落库隐藏推理/duration 与 cost 观测字段）、`test_sse_cursor` 4 项（重连带游标先快照再回放游标后事件/不带游标回放全部/游标末尾只发快照/live 消息事件 id 为持久 sequence）；既有测试适配（test_loop 断言不再发布 agent.thought，runtime/test_runtime_lease fixture mock recorder 以隔离 DB 逻辑，集成测试校验 agent_events 单调 sequence、事件类型覆盖 phase.enter/tool.request/tool.result、禁止字段与 SSE 事件 id=sequence）。范围说明：`agent_memory_entries`（ReAct 工作记忆，含 thought）按 DATABASE.md §2.2 方向仍保留用于内部断点续跑，其删除与工作集重建依赖 §13.4，留待后续切片；agent_events 与 §15 全部要求已落地。ADR 检查 1–8：否（实现已确认的 DATABASE.md §5.4/§9/§11 与 RESEARCH_PIPELINE §15/§16/§17.3-22 语义，不改变服务边界/公共契约兼容性/安全/数据生命周期；删除 agent.thought 为让实现重新符合 §16 既有规范的缺陷修复，不加新长期约束）。（2026-08-06）
- 修订前端视觉方向为「浅色默认 + 深色可选」（规范变更，负责人批准，2026-08-06）：默认浅色办公主题，登录抽屉与登录后的工作区默认浅色；入口全屏叙事页固定保留品牌深色叙事，不随主题切换；账号菜单新增「主题选择」，切换立即生效并持久化到 `localStorage`（键 `evidsight-theme`），刷新与重新登录后保持。同步修订 `apps/web/docs/UIDESIGN.md`（设计方向/目标、主题机制与深浅两套色彩 Token、背景材质、按钮与输入框的主题适配、视觉验收清单、原型基线）与 `apps/web/docs/FRONTEND.md`（§3.3 账号菜单、§4.4 主题与外观、§5.1 登录默认浅色、§14 验收门禁）。ADR 检查 1–8：否（纯前端视觉方向与客户端主题偏好：不涉及服务边界、公共契约、数据与安全、核心机制或不可逆变更；浅色默认与主题切换均为低成本、可回滚的客户端实现）。主题切换交互已在 `.superpowers/prototypes/evidsight-web` 落地并验证；`resource/prototype` 按主题分目录归档 `dark/` 与 `light/` 各 20 张截图（深色基线 + 浅色默认），README/FRONTEND/DEVELOPMENT 引用同步指向默认浅色版。纯文档变更，已执行格式与链接检查。（2026-08-06）
- 落地 M3 切片 E「租约接入 AgentRuntime」：将切片 D 的租约协议接入 `AgentRuntime`（对齐 RESEARCH_PIPELINE §13.1/§13.2、DATABASE.md §8、ADR-008，落实 §17.12「两个 Worker 竞争时只有当前 lease generation 可提交 Step」）。① 领取——`start_research_task` 在 pending 正常路径与 running 崩溃恢复路径统一领取新 generation（`claim_task_lease` 条件更新，非终态/未取消/租约为空或过期才成功），领取失败（并发 Worker 或扫描器已接管）即放弃启动并释放锁，防止双 Worker 同时执行；领取成功经 `TaskLockHandle.bind_lease` 绑定 `worker_id`/`lease_generation` 供续租与提交校验。② 续租与释放——`TaskLockHandle` 在刷新循环内续租 Redis 锁 TTL 与 DB 租约（`renew_lease` 独立会话、owner+generation 匹配才可续，续租失败即停止刷新）；`release()` 先释放 DB 租约（`release_lease` 仅 owner 匹配清除 lease_owner/lease_expires_at）再释放任务级锁，终态任务无有效租约。③ 提交门禁——`_complete_step`/`_fail_step` 提交业务结果前经 `is_step_commit_allowed` 校验（Task 的 owner+generation 仍匹配、未终止或取消才放行），失去租约的 Worker 抛 `LeaseLostError` 立即停止、不提交业务结果。④ 终态所有权——`_finalize_task` 推导终态前经新增 `is_task_ownership_valid` 校验所有权（与 `is_step_commit_allowed` 的差异：取消已请求时允许推导 canceled/partially_completed，因此不拦截 `cancel_requested_at`），防止被恢复 Worker 接管的旧 Worker 覆盖终态；`_handle_fatal_error` 对 `LeaseLostError` 不写 failed 终态（交给 Recovery Scanner 接管），并新增 canceled 终态的 `task.canceled` SSE 发布。⑤ 取消检查点修正——取消接口只写 `cancel_requested_at`（§13.2），`_is_task_canceled` 从旧 `status==canceled` 修正为检测 `cancel_requested_at`，取消在迭代检查点生效、安全停止后进入 `_finalize_task` 由 TaskStateResolver 推导终态（修正切片 D 后 run() 内旧短路分支导致的取消任务卡死问题）。验证：RED 确认（`LeaseLostError` 缺失 + `TaskLockHandle` 无租约绑定/领取导致 11 项新增用例失败）；docker 容器内 research 全量 unit（非 slow）865 通过、1 跳过、0 失败（`test_llm` 1 项为本机根 `.env` 覆盖 `LLM_MODEL` 的环境依赖，与本切片无关）、AgentRuntime 集成测试 2 项通过、research 契约 73 项 + `packages/contracts/tests` 131 项全绿、Architecture 17 项全绿；新增 `test_task_lease_wiring` 5 项（pending/running 启动领取并绑定 handle、恢复路径 generation 递增、领取失败放弃启动并释放锁、release 清除 DB 租约、renew 续租）与 `test_runtime_lease` 6 项（租约匹配允许提交/丢失拒绝提交抛 LeaseLostError、失败 Step 丢失拒绝写入、LeaseLostError 不写 failed 终态、取消请求停止循环进入最终化、未取消正常进入最终化）；既有 `test_runtime` 适配（fixture 绑定租约放行提交、取消用例改为断言进入最终化）。ADR 检查 1–8：否（实现已接受的 ADR-008 与 RESEARCH_PIPELINE §13、DATABASE.md §8 语义，不改变服务边界/安全/数据生命周期）。（2026-08-06）
- 落地 M3 切片 D「租约 + 取消 + 恢复数据层与协议」：① 数据层（对齐 DATABASE.md §5.1/§8、RESEARCH_PIPELINE §13、ADR-008）——`research_tasks` 新增取消请求列 `cancel_requested_at`、租约列 `lease_owner`/`lease_expires_at`/`lease_generation`、恢复列 `recovery_count`/`last_completed_step_id`，以及 Worker 领取与恢复扫描索引 `idx_status_lease_expires`(status, lease_expires_at)；迁移 `d1e2f3a4b5c`（down_revision=c0d1e2f3a4b5）经真实 MySQL 全链验证（upgrade head 通过、downgrade 回滚后 5 列/索引删除、再 upgrade 往返可用）。② 租约协议原语（§13.1）：`claim_task_lease`（单条条件更新：非终态、未请求取消、租约为空或已过期才领取，写入 lease_owner/lease_expires_at 并递增 lease_generation）、`renew_task_lease`（仅当前 owner 且 generation 匹配可续）、`release_task_lease`（仅 owner 匹配释放）、`is_step_commit_allowed`（Step 提交前校验 Task 的 owner+generation 仍匹配、未终止或取消——过期 Worker 迟到提交返回 False，不得覆盖恢复 Worker 结果）。③ 取消请求化（§13.2）：`cancel_task` 不再直接 CAS 改写 `canceled` 终态，只持久化 `cancel_requested_at`（pending/running 且未请求过才写；重复取消幂等返回当前状态；终态抛 E2003）；`ResearchCancelResponse` 增加 `cancel_requested`，`status` 保持当前值；TaskStateResolver 在 Worker 安全停止后推导终态（全部 phase 完成→completed、取消后证据足够→partially_completed、否则→canceled）；API `POST .../cancel` 返回「任务已请求取消」，SSE `task.canceled` 载荷改为携带 `cancel_requested=true` 与当前 status，终态事件由 Worker/Resolver 在安全停止后发布。④ Recovery Scanner 按租约扫描（§13.5）：改为按 `(status='running', lease_expires_at 为空或已过期)` 查找候选（替换原 started_at 阈值）；条件领取再次确认租约过期、将遗留 running Step 置为 retrying、清除旧 owner 并递增 `recovery_count`、重新投递到 `research.execute`（与 API 创建共用执行队列）；启动扫描/周期扫描/手动恢复复用同一 `recover_stale_tasks`。⑤ 配置：新增 `RESEARCH_TASK_LEASE_TTL_SECONDS`（默认 300，约束续租周期<租约/2、扫描间隔≤租约），同步 `.env.example`。验证：RED 确认（租约原语缺失 + cancel_requested_at 列缺失 + 恢复仍按 started_at 扫描导致用例失败）；docker 容器内 research 全量 unit（非 slow）854 通过、1 跳过、0 失败（`test_llm` 1 项为本机根 `.env` 覆盖 `LLM_MODEL` 的环境依赖，与本切片无关）、契约 73 项全绿、Architecture 17 项全绿、research-api 容器健康；新增 `test_task_lease` 15 项（领取成功/有效租约拒绝/过期递增/终态与取消拒绝、续租 owner 与 generation 约束、释放 owner 匹配、is_step_commit_allowed 六类放行/拒绝）、`test_cancel_request` 7 项（running/pending 只写 cancel_requested_at、重复取消幂等、终态 E2003、Resolver 三态推导）、`test_recovery_lease` 7 项（租约过期投递/未过期跳过/锁存在跳过/清 owner 递增计数/遗留 running Step 置 retrying/非 running 不投递/无租约视为过期）；既有 `test_startup_recovery` 适配租约语义（阈值内任务显式持未过期租约、非 running 不投递；测试会话上下文管理器将 commit 重定向为 flush 防止跨测试数据泄漏）。ADR 检查 1–8：否（实现已接受的 ADR-008 与 RESEARCH_PIPELINE §13、DATABASE.md §5.1/§8 语义，不改变服务边界/安全/数据生命周期）。（2026-08-06）
- 落地 M3 切片 C「Rerank 内部候选接入 + Evidence 分型」：① 数据层（对齐 DATABASE.md §6.2 / ADR-009）——`evidence_items` 新增 `source_type`（internal|web 默认 web，存量回填 web）、内部稳定身份列（knowledge_base_id/document_id/document_version_id/segment_id）、显示快照（document_display_name_snapshot/display_title/location_summary）、时间（source_observed_at）、score_summary、validity（available|restricted|missing|stale 默认 available）与 Web 快照列（canonical_url_snapshot/fetched_at_snapshot）；`content`/`source_id` 改为可空（internal 证据无正文、不引用 web source），新增内部唯一键 `uq_evidence_internal_identity`；迁移 `c0d1e2f3a4b5`（down_revision=a5b6c7d8e9f0）经真实 MySQL 全链验证（upgrade head 通过、downgrade 回滚后新增列/约束删除、再 upgrade 往返可用；存量 10 条证据回填 web 且 content 保留）。② Rerank 策略感知（RESEARCH_PIPELINE §7）：`run_rerank` 按 `source_strategy` 加载候选——`knowledge` 从 Search step.output 读 `internal_candidates` 并经 `resolve_retrieval` 按稳定身份重取当前正文（仅当前 Step 内存），`web` 保持既有 research_sources 路径（回归），`hybrid` 合并内部候选与 Web BM25 候选统一精排；产出 internal EvidenceItem（source_type=internal、content NULL、source_id NULL、内部稳定 ID/显示名/位置/观察时间/score_summary/validity=available 完整），web EvidenceItem 保持既有行为；resolve 失败（KB_FORBIDDEN→E3115/契约错误→E3117/瞬时不可用重试耗尽→E3116）fail-closed，knowledge 绝不降级 Web；新增 `_load_internal_candidates`/`_resolve_internal_candidates`/`_internal_to_candidates` 与 Candidate/Evidence dataclass 分型字段。③ 下游消费 internal 证据（RESEARCH_PIPELINE §8.1/§9/§11）：Synthesis 对 internal Evidence 经 resolve 重取正文构造当前 Step 工作集、prompt 区分「内部来源」/「来源标注」且不持久化正文；Evidence Graph 条目携带 source_type/显示名/位置、无正文，来源聚合按 Document 身份区分内部与 Web；Render prompt 区分内部与外部来源标注，internal 引用不内嵌正文。验证：RED 确认（resolve_retrieval 缺失 + source_type 模型字段缺失导致用例失败）；docker 容器内 research 全量 unit（非 slow）825 通过、1 跳过、0 失败（`test_llm` 1 项为本机根 `.env` 覆盖 `LLM_MODEL` 的环境依赖，与本切片无关）、契约 73 项全绿、Architecture 17 项全绿、research-api 容器健康；新增 `test_rerank_internal` 5 项（knowledge 内部候选产出 internal 无正文证据/空候选失败关闭/resolve fail-closed 不降级、hybrid 合并产出两类证据、模型分型字段）与 `test_downstream_internal` 3 项（Synthesis resolve 重取区分来源不持久化、Graph 无正文带 source_type、Render 区分来源不内嵌正文）。ADR 检查 1–8：否（实现已接受的 ADR-003/008/009/010 与 RESEARCH_PIPELINE §7-9/§11、DATABASE.md §6.2 语义，不改变服务边界/安全/数据生命周期）。（2026-08-06）
- 落地 M3 切片 B「Knowledge Search Tool」：实现 Internal Retrieval Consumer 客户端（`app/core/internal_retrieval_client.py`，search/resolve 双端点）——请求携带 Service JWT / `X-EvidSight-Contract-Version` / `X-Request-ID` / `traceparent`，载荷只含契约字段（purpose 固定 `research_retrieval`/`research_evidence_resolve`，不携带角色/授权结论）；响应严格校验（`returned_count==len(results)`、命中含完整稳定身份）；错误映射（`KB_FORBIDDEN`→`E3115 InternalKnowledgeForbidden` fail-closed、`AUTH_USER_DISABLED`→`E1010`、`INTERNAL_CONTRACT_*`/`INTERNAL_SERVICE_UNAUTHENTICATED`/`EVIDENCE_SOURCE_UNAVAILABLE`→`E3117 InternalRetrievalContract` 不重试、`INTERNAL_RETRIEVAL_UNAVAILABLE`/限流/网络/超时→`E3116 InternalRetrievalUnavailable` 有界指数退避重试）；并落地策略感知 search 阶段（`searcher.run_search` 按 `source_strategy` 分流）：`knowledge` 只走 Internal Retrieval（不调用 Tavily、不创建 Web Source、`KB_FORBIDDEN` fail-closed 不降级 Web）、`web` 保持既有 Tavily 行为（回归）、`hybrid` 先内部检索再 Web 搜索且 Web Query 只来自原始 Topic/公开子问题、内部标题与 excerpt 绝不进入 Web Query（ADR-010 域隔离）；内部命中以 `RetrievalHit.to_safe_dict()` 持久化（稳定 KB/Document/Version/Segment ID、位置、评分摘要、时间，删除 `minimal_excerpt` 与等价正文）；新增配置键 `EVIDSIGHT_INTERNAL_RETRIEVAL_TIMEOUT_SECONDS`/`EVIDSIGHT_INTERNAL_RETRIEVAL_RETRY_MAX`。验证：RED 确认（客户端模块缺失 + 策略分流缺失导致用例失败）；docker 容器内 research 全量 unit（非 slow）817 通过、1 跳过、0 失败（`test_llm` 1 项为本机根 `.env` 覆盖 `LLM_MODEL` 的环境依赖，git stash 后仍失败，与本切片无关）、契约 73 项全绿；新增 `test_internal_retrieval_client` 13 项（请求头/载荷/错误映射/重试/响应校验）、`test_knowledge_search` 8 项（三类策略分流、fail-closed、域隔离、excerpt 禁入持久化）。ADR 检查 1–8：否（实现已接受的 ADR-010 与 RESEARCH_PIPELINE §3/§5.3/§6.1/§12.2 语义，不改变服务边界/安全/数据生命周期）。（2026-08-06）
- 落地 M3 切片 A「Research 接入内部知识」#7（Worker 来源策略 fail-closed 守卫）：`_run_pipeline` 在执行前按 DATABASE.md §5.1 不变量核验来源策略依赖——`knowledge`/`hybrid` 任务必须持有至少一个 `research_task_knowledge_bases` 选择行，不变量被破坏时任务直接终态 `failed`（`E3114 KnowledgeBasesMissing`，`recoverable=false`，`completed_at` 落库，发布 `failed` 状态转换指标），不得静默按 `web` 路径执行或部分放行；`web` 策略不受守卫影响。新增 `exceptions.KnowledgeBasesMissingException`（E3114）与 `_enforce_strategy_dependency`，复用 `emergency_fail_task` CAS（pending/running→failed）。API.md §8.1 记录守卫语义与 E3114、ADR 检查 1–8 否。新增 `TestRunPipelineStrategyGuard` 6 项（knowledge/hybrid 各有/无 KB、web 无 KB 对照、多 KB 放行）。验证：RED 确认（仅缺守卫的 2 个 fail-closed 用例失败）；docker 全栈 research 全量 unit（非 slow）797 通过、1 跳过、0 失败（+6 新用例）。（2026-08-05）
- 落地 M3 切片 A「Research 接入内部知识」#4–#6：① 数据层（#4）——`research_tasks` 新增 `source_strategy`（knowledge/web/hybrid 默认 web）、`idempotency_key`/`request_fingerprint` 与唯一约束 `uq_research_tasks_user_idempotency(user_id, idempotency_key)`，新增 `research_task_knowledge_bases` 表（PK (task_id, knowledge_base_id)、FK CASCADE、`selection_order` 任务内唯一、`display_name_snapshot`）；迁移 `a5b6c7d8e9f0`（down_revision=33ad807896b2）经真实 MySQL 全链验证（`research_migration_test` 库 upgrade head 15 迁移全过、downgrade 33ad807896b2 回滚后三列/唯一约束/KB 表删除、再 upgrade 往返可用，末尾 aiomysql 循环关闭噪音为良性 teardown）。② 请求契约（#5）——`ResearchCreateRequest` 增加 `source_strategy`/`knowledge_base_ids` 与组合校验（knowledge/hybrid ≥1 个合法 UUID、web 不接受 KB、≤50），`create_task` 按策略分流（knowledge/hybrid 跳过意图识别直进 Pipeline、直接回答强制 `source_strategy=web`），`tests/unit/services/test_source_strategy.py` 11 项全绿。③ 幂等创建（#6）——新增 `POST /api/v1/research/tasks`（202）：`Idempotency-Key` 必填（≤128），按规范化载荷 SHA-256 指纹；同用户同 Key 同指纹返回原任务（`idempotent_replayed=true`，不重复分发）不同指纹 409 `E2009 IdempotencyKeyConflict`；重放响应返回任务当前状态；`ResearchCreateResponse` 增加 `idempotent_replayed`；新增 `app/api/research_v1.py` 与 main 挂载 `/api/v1/research`，nginx 新增 `/api/v1/research/` 路由到 research-api；新增 `tests/unit/api/test_research_v1_create.py` 9 项全绿。验证：docker 全栈（挂载 workspace + 根 `.env` TAVILY_API_KEY）research 全量 unit（非 slow）791 通过、1 跳过、0 失败；本地 research venv `test_source_strategy` 11、`test_research_service` 60、`test_research_v1_create` 9、`test_research` 50 全绿。修复既有测试基建缺陷：`tests/unit/conftest.py` 新增 `_celery_delay_noop` autouse fixture（单元测试无 Redis broker，将 API 层 `_execute_research_task.delay()` 替换为 no-op，否则 POST 创建任务的 API 用例在 kombu `retry_over_time` 无限重试挂起）；`tests/conftest.py` 的 `event_loop` fixture 补充 `__original_fixture_loop` 标记（对齐 pytest-asyncio 0.24 自带约定，避免弃用警告分支 `inspect.getsourcelines()` 在容器挂载环境抛 OSError）。ADR 检查 1–8：否（契约与幂等机制由 API.md §8、DATABASE.md §5.1–5.2、ADR-008 唯一确定）。（2026-08-05）
- 实现 M2 审查发现项修复中的新增能力：P2 生产配置 fail-fast 与就绪探针（`app/core/startup_checks.py` 新增 `validate_production_config`，生产环境 AUTH_ALLOWED_ORIGINS 必填、Service 公钥可加载校验，lifespan 缺失即拒绝启动；`service_security` 新增 `public_keys_loadable`；新增 `/api/health/ready` 就绪端点，公钥不可加载返回 503，`/api/health` liveness 不变，auth 中间件放行 ready 路径）；P4 发布后在线集合一致性校验（`BaseVectorStore` 新增抽象 `get_ids`，`ChromaVectorStore` 分页取回 ids；`publish_version` 在删旧版本向量后校验新版本向量全集与 staging 声明一致，缺失即抛错进入 `recovering` 由恢复器补齐/回滚，不静默接受丢失）。验证：`test_startup_checks.py` 3 项、`test_health_ready.py` 2 项、`test_service_security.py::TestPublicKeysLoadable` 3 项、`test_versioning.py` 在线集合不一致→recovering 1 项、`test_vector_store.py::TestChromaVectorStoreGetIds` 2 项全绿。P4 真机 Chroma 在线集合校验与 2C2G 演练需 docker 环境，本轮以 fake store 单测覆盖，真机验证延后。（2026-08-05）
- 记录 v1.x / P2 排期新增（ROADMAP §5）：Excel/CSV 文档入库与可检索态列为 v1.x（P1）扩展 FR-KB-002 支持格式；执行通道 Execution Lane 执行器契约列为 P2 远期，首个执行器为 excel-ai-analyst（公式校验/勾稽/推演），作为「通用 Agent、插件和工作流市场」的早期形态。仅排期记录，未进入 v1.0 范围，未投入实现。（2026-08-05）
- M2 真实环境验收与部署缺口修复（AC 跑分 + 异常演练）：供给 dev Service JWT 密钥并接线 compose（`deploy/secrets/service-jwt/`，git 忽略；Research RS256 签发 / Knowledge 公钥验证，`EVIDSIGHT_PLATFORM_SERVICE_JWT_TTL_SECONDS=300`），修复此前 compose/.env 从未配置密钥材料导致 Internal Retrieval 服务认证在部署中不可用的问题；新增 `knowledge-beat` 服务（复用 worker 镜像，独立 `/var/lib/celery` volume），使 `scan_stuck_versions` 恢复扫描每 60s 在部署中真实运行。AC-005 固定文档集入库成功率 100.00%（19/19 已评估，20 样本 1 次瞬时上传非 201 按规范排除、手动重传成功，门槛 ≥99%）、AC-006 固定评估集 Recall@5=1.0000（10/10，门槛 ≥0.85）、AC-010 来源可追溯率 100%（5/5，门槛 100%）全部通过。异常演练实测 Worker 中断/租约恢复全链路：SIGKILL 中断于 `parsing` → 版本冻结 + 锁残留（Redis `version_lock:*` TTL 600s）→ 扫描检测 stuck 但尊重活跃锁不重投 → 锁过期后重投 `ingest_version` → 断点续跑至 `ready`（11 chunks 无重复、恢复后向量可检索、KB 锁释放）；并实测发现修复恢复扫描漏掉 `queued` 状态的实现缺陷（对齐 RAG_PIPELINE §3.4「queued 或超时非终态由扫描恢复」，`SCAN_STATUSES = PROCESSING_STAGES ∪ {queued}`，新增验收测试）。AC-005/006 验证脚本实测发现并修复三个缺陷（`_list_documents` 误按顶层 `data` 迭代、`evaluated` 用 Path 对象比对字符串键、AC-006 recall 未按期望文档去重导致 >1 伪值）。验证：容器内 Knowledge 全量 1513 项全绿；新增固定评估集 `services/knowledge/tests/eval/eval_test_set.json`（10 题）。ADR 检查 1–8：否（除恢复扫描 `queued` 覆盖为规范对齐缺陷修复外，均为配置/验证入口/演练，不改变服务边界/安全/数据生命周期）。（2026-08-05）
- 实现 M2 版本化写路径（入库/重处理/删除/失败恢复）并完成 `documents.status` 枚举 6 值对齐（M2 收官）：`DocumentStatus` 完整迁移为 `queued|processing|completed|partial|failed|deleting`（Alembic `c6d7e8f9a0b1` 存量映射 uploaded/parsing/chunking/embedding/vector_storing→processing、success_with_warnings/partial_failed→partial，并按 active_version 对应版本终态同步；`documents` 默认值改 `queued`；API 反向兼容旧状态名查询）。新增 `app/ingest/versioning.py`（版本创建/递增/Checkpoint/pending 查询、`map_document_status` 对外映射、`publish_version` 原子发布：KB `index_status=updating` + `index_generation++` → 写版本作用域向量 → MySQL 事务切换 `active_version` → 删旧版本向量 → 清理 staging → `ready` → BM25 失效）与 `app/ingest/recovery_tasks.py`（Celery Beat 每 60s `scan_stuck_versions`：非终态版本超 `STUCK_VERSION_TIMEOUT` 且无活跃锁重新投递 `ingest_version`、KB 发布锁超 `KB_LOCK_TIMEOUT` 回滚 `ready`，均显式路由到 ingest 队列）。`app/ingest/tasks.py` 重构为 Version 驱动（Worker 以 Version UUID 为幂等键、阶段状态机 `queued→parsing→chunking→embedding→indexing→verifying→ready` 断点续跑、Embedding 批次级增量 staging 产物、Chunk 写入 `document_version_id`+稳定 `segment_uuid`、Chroma id 版本作用域 `doc_{doc_id}_v{version}_c{chunk_index}`）；`document_service` 的 upload/reprocess/delete 适配版本语义（reprocess 允许 completed/partial/failed 终态，禁 queued/processing 并行版本）；删除保留版本感知（按 doc_id 清理全版本向量 + FK CASCADE）。BM25 只索引 Active Version chunks（JOIN document_versions/documents 过滤 `active_version`），缓存 Key 含 `index_generation`（publish 递增后自动失效），invalidate 按前缀删除该 KB 全部世代 key。Internal Retrieval 对 `updating` KB 做 `PUBLISH_LOCK_WAIT_MS` 有界等待后仍不可用才返回可重试 503，`recovering` 直接 503。前端文档状态标签/筛选/样式对齐 6 值枚举。新增配置键 `PUBLISH_LOCK_WAIT_MS`/`STUCK_VERSION_TIMEOUT`/`KB_LOCK_TIMEOUT`。AC-005/006/010 验证脚本落地（`services/knowledge/scripts/verify_ac005_ingest_success.py`、`verify_ac006_recall.py`、`verify_ac010_traceability.py`，对齐 TESTING.md §7 发布记录模板，缺失评估数据报错退出、不伪造完成）。验证：容器内 Knowledge 全量 1512 项、Architecture 17 项、前端 build 与 539 项测试全绿；新增版本化写路径验收 21 项、恢复扫描 7 项、BM25 Active Version/generation 及 bounded wait 测试；迁移离线 SQL 校验通过；Research Consumer 契约 73 项通过（research 服务零改动，其余 research 失败为本机缺失 TAVILY_API_KEY/Redis 的环境依赖，与本切片无关）。ADR 检查 1–8：否（实现已接受的 ADR-007，不改变服务边界/安全/数据生命周期）。（2026-08-05）
- 实现权限感知的 `/internal/v1/retrieval/search` 与 `/resolve` Provider 端点（M2 权限感知检索切片，含版本化数据层）：按 DATABASE.md §5.3 / ADR-007 落地 `document_versions` 表（`document_version_status` 状态机、Checkpoint、`published_at`）与 `documents.active_version`/`display_name`、`knowledge_bases.index_status`/`index_generation`、`chunks.segment_uuid`/`document_version_id`、`sections.document_version_id`；Alembic 迁移 `f0a1b2c3d4e5` 对存量回填（62 文档建立版本 1 并按文档状态映射 `ready`/`ready_with_warnings`/`failed`/`queued`、可检索文档置 `active_version=1`、1224 chunk 回填稳定 `segment_uuid` 与版本归属、1064 section 归属版本，0 孤儿 chunk）。端点按 contracts/README.md §3 固定顺序校验（Research 服务身份 → Contract 版本与结构 → 用户 active → 逐 KB READ → 检索/解析）：任一无权 KB 整次拒绝（`KB_FORBIDDEN` 403）、任一 KB 索引未就绪返回可重试 503（`INTERNAL_RETRIEVAL_UNAVAILABLE`）、命中仅映射 Document Active Version 且要求 `segment_uuid`、引用不可用整批失败（`EVIDENCE_SOURCE_UNAVAILABLE` 400）；命中对象仅含契约字段，不泄露 ORM/路径/缓存/Embedding。请求过滤器：`filters.document_ids` 在真实检索路径按 Document UUID 过滤，非法过滤器结构返回 `INTERNAL_CONTRACT_INVALID`；`languages`/`updated_since`/`updated_until` 的语义应用因当前数据模型无语言标签与统一检索时间口径而留待后续切片（结构校验已落地，不做静默错误）。入库写路径版本化由文档入库/重处理/删除切片落地。验证：容器内 Provider 端点级验收 23 项、Knowledge 契约 38 项、知识服务全量 1495 项全绿；迁移对 dockerized MySQL 应用并完成回填一致性核对。ADR 检查 1–8：否（实现已接受的 ADR-007 版本化生命周期与 ADR-002/005 检索权限语义，不改变服务边界/安全/数据生命周期）。（2026-08-04）
- 落地 Internal Retrieval 与 Evidence Contract Schema/Fixture 与验收测试（契约先行，M2）：新增 7 个 Schema（retrieval-request/response、retrieval-hit、evidence-resolve-request/response、evidence-reference、evidence-relation）并扩展 `common.schema.json`（`Timestamp`/`ScoreKind`/`UnitInterval`/`InternalSourceIdentity`/`SourceLocation`）与 `error-response.schema.json`（新增 `KB_FORBIDDEN`/`INTERNAL_RATE_LIMITED`/`INTERNAL_RETRIEVAL_UNAVAILABLE`/`EVIDENCE_SOURCE_UNAVAILABLE` 四个错误码）；落地 24 valid / 50 invalid 固定 Fixture；新增共享语义不变量 `generated/python/evidsight_contracts/semantics.py`（纯 JSON Schema 无法表达的跨字段约束：`returned_count == len(results)`、`updated_since <= updated_until`、`source_type` 与来源身份匹配）与 7 个 Pydantic 参考模型；契约自检测试改为动态遍历全部 Schema，`test_refs_resolvable` 修正为禁止未重写相对 `./` 引用（同文档 `#/` 片段引用合法）。验证：`packages/contracts/tests/` 131 passed、Research Consumer 73 passed（Research `.venv` pytest 8.3.5）、Knowledge Provider 15 passed（Knowledge `.venv` jsonschema 4.26），既有 Identity Status 契约测试无回归。ADR 检查 1–8：否（不触发——实现已确认的 `contracts/README.md` 设计，不改变服务边界/安全/数据生命周期）。Provider 端点级测试由权限感知 Provider 切片落地。（2026-08-04）
- 接受 ADR-008/009/010（M3 进入条件核验，2026-08-05）：负责人裁决「创建 ADR（A/B/C 三份）」并分别评审接受为 `accepted`——ADR-008 命中检查项 5、6，ADR-009 命中 3、7，ADR-010 命中 4（详见各 ADR 文件）；M3 进入条件三项核验通过（M2 验收记录、Research 全栈运行基线、三类来源策略与 Evidence Contract 对齐），并裁决 Research API 路径按 API.md 迁移到 `/api/v1/research`；同步 decisions 索引与 ROADMAP M3。（2026-08-05）
- 接受 ADR-007：Knowledge 文档版本化生命周期与删除一致性——每次入库/重处理创建独立 `document_version` 并以阶段 Checkpoint 为恢复点（MySQL 为唯一权威），Embedding 先入 staging、发布以 per-KB `index_status` 短时锁原子切换 `active_version` 并清理旧向量，删除走 KB `deleting` 幂等清理与 BM25 缓存失效；检索权限语义由 ADR-002/005 交叉引用覆盖，不重复定义；ADR 检查项 4、5 命中，负责人于 2026-08-04 接受为 `accepted`；同步 decisions 索引、RAG_PIPELINE §16 门禁记录、DATABASE §9 与 API §6.2 交叉引用。（2026-08-04）
- M1 退出门禁 6 负向日志测试补齐：新增 `services/knowledge/tests/unit/services/test_auth_service.py::TestLoginLogSensitivity`，断言登录成功与失败路径日志均不含密码明文、登录成功日志不含 Access/Refresh Token 明文；负向验证确认在 `auth_service.login` 注入密码日志时测试正确 RED，GREEN 无生产代码修改（目标行为由既有实现满足）；同步 TESTING.md §3.2 与 IDENTITY_AND_ACCESS ADR 检查记录。（2026-08-04）
- 补齐知识库权限矩阵验收测试：新增 `services/knowledge/tests/unit/core/test_permissions.py` 纯函数矩阵测试，以 PRD §8.2 五列矩阵驱动 `require_kb_readable`（READ 由 visibility 优先：public→所有登录用户、private→owner+admin）、`require_kb_writable`（WRITE 由 ownership 决定、admin 治理覆盖）与 `require_kb_owner`（上传文档 owner-only、admin 不可代传），共 15 个用例；容器内负向验证确认移除 admin 分支或破坏 owner 检查时对应测试正确 RED，GREEN 无生产代码修改（目标行为由既有 `permissions.py` 满足）；同步 TESTING.md §3.2 与 IDENTITY_AND_ACCESS ADR 检查记录。（2026-08-04）
- 补齐 IA-010 签名密钥轮换验收测试：`test_service_security.py` 新增 `TestServiceKeyRotation`，验证 Service JWT 双 Key 验证窗口内新旧 Token 均按 Key ID 通过、窗口后移除旧 Key 旧 Token 失效且新 Token 仍有效、切换签发侧使用新 Key ID 后按新 Key 验证通过；容器内负向验证确认测试可检测轮换窗口支持缺失，GREEN 无生产代码修改（目标行为由既有 `verify_service_token` 多 Key 映射满足）；同步 TESTING.md §3.2 与 IDENTITY_AND_ACCESS ADR 检查记录。（2026-08-04）
- 补齐 IA-005 禁用用户全链路 API 层验收测试：新增 `services/knowledge/tests/unit/api/test_disabled_user_access.py`，依赖层验证 `get_current_user` 对不存在/禁用用户统一抛 `E5010`，API 层验证禁用用户对 Chat 创建、单个/批量上传、重处理、治理写操作（禁用/启用用户、重置密码）均返回 401 `E5010` 且不进入业务逻辑；容器内负向验证确认测试可检测禁用检查缺失，GREEN 无生产代码修改（目标行为由既有 `get_current_user` 满足）；同步扩充 TESTING.md §3.2 与 IDENTITY_AND_ACCESS §13 IA-005 场景表述。（2026-08-04）
- 实现 Service JWT 基础设施与 Internal Identity Status Provider（IA-012）：Research 以 RS256 私钥签发服务身份 Token（`create_service_token`），Knowledge 以公钥验证（`verify_service_token`，Issuer/Audience/Key ID/时间窗口）；Knowledge 落地 `GET /internal/v1/identity/users/{id}/status` Provider 端点，按 API.md §11.1 顺序校验，返回契约 `IdentityStatusResponse`（1.0.0），错误统一使用 `error-response.schema.json` 信封；`users` 新增 `status_version` 列并在状态变更时递增；`auth_middleware` 豁免 `/internal/v1/*`；测试密钥动态生成，不提交仓库。（2026-08-04）
- 实现 Research 身份状态门禁（IA-012 身份状态契约 Research 侧）：新增 `app/core/identity_status_client.py`（`check_user_status`，调用 `GET /internal/v1/identity/users/{id}/status`，携带 Service JWT、Contract 版本、`X-Request-ID` 与 W3C `traceparent`）；`create_task` 创建任务前实时复核用户状态，用户禁用/不存在 → `UserDisabledException`（E1010/401）、身份库不可用/网络/超时/意外响应 → `ServiceUnavailableException`（E9002/503），失败关闭且不分发 Worker；新增配置键 `EVIDSIGHT_KNOWLEDGE_INTERNAL_BASE_URL` 与 `EVIDSIGHT_IDENTITY_STATUS_TIMEOUT_SECONDS`。（2026-08-04）
- 接受 ADR-006：Refresh Token 浏览器传输收口为 HttpOnly Cookie + double-submit CSRF，`login`/`refresh` 不再返回 Refresh Token 明文，`refresh`/`logout` 新增必需 `X-CSRF-Token`；旧 body `refresh_token` 仅作为配置开关控制的迁移期兼容入口。（2026-08-03）
- 实现事件③ Refresh Token Cookie/CSRF 收口后端切片（IA-015/IA-016）：新增 CSRF 基础设施与 Cookie 读写（`app/core/csrf.py`：`generate_csrf_token`/`verify_csrf` double-submit 校验与 Origin 白名单、`set_refresh_cookie`/`set_csrf_cookie`/`clear_auth_cookies`）及 7 个 `EVIDSIGHT_PLATFORM_*` 配置键；v1 登录/刷新/退出改用 HttpOnly Refresh Cookie + `X-CSRF-Token`（`login` 响应不含 `refresh_token` 明文并设置 Refresh/CSRF Cookie、`refresh` 从 Cookie 读取并原子轮换且同响应轮换 Cookie、`logout` 幂等撤销并清除 Cookie，刷新失败按 ADR-006 清除 Cookie）；`EVIDSIGHT_PLATFORM_AUTH_BODY_REFRESH_COMPAT` 开启时允许 Refresh Cookie 缺失回退 body `refresh_token` 并记录不含 Token 的弃用日志。（2026-08-03）
- 实现事件③ Refresh Token Cookie/CSRF 收口前端切片（IA-015/IA-016）：`apps/web` 登录/刷新/退出切换至 v1 端点（`/api/v1/auth/login` 消费 unwrapped `LoginV1Response`；`/api/v1/auth/refresh` 与 `/api/v1/auth/logout` 从非 HttpOnly CSRF Cookie 读取并以 `X-CSRF-Token` Header 回传、不带 body `refresh_token`）；Axios 实例启用 `withCredentials` 携带凭据；前端不再读取/保存 Refresh Token 明文（移除 `localStorage.refresh_token` 读写与 Pinia `refreshToken` 状态，仅在清态时保留遗留键清理）；刷新失败（CSRF 缺失/不一致、Refresh Cookie 过期/吊销）统一按刷新失败处理清除 Access Token 与用户状态并返回登录入口。（2026-08-03）
- e01 外部 DTO 整数 ID 只读取证：分类 A 类用户 `id`（6 处改 UUID）、B 类外部响应用户引用（6 处移除内部 `users.id`）、C 类内部资源 ID（4 处保留 BIGINT）；为 DTO UUID 迁移事件的规范与验收提供输入。（2026-08-03）
- 落地 Internal Identity Status 契约最小可执行集合：`schemas/v1/`（common / identity-status-response / error-response）、Fixture（identity-status-response 1 valid + 12 invalid，error-response 2 valid + 3 invalid）、Pydantic 参考模型与 `loader.py`（`$ref` 文件系统解析 + `referencing.Registry`）、契约自检与 Knowledge Provider / Research Consumer 契约测试；Provider 端点 `GET /internal/v1/identity/users/{id}/status` 当前为正确 RED。（2026-08-03）
- 建立文档治理指南，统一实现偏离、实现领先、迁移态和权威文档冲突的取证、裁决、状态与退出门禁。（2026-08-03）
- 建立 EvidSight PRD、总体架构、身份与访问、API、Database、Knowledge/Research Pipeline、Frontend 和 UI 规范基线。（2026-08-01）
- 建立 Monorepo 迁移计划、开发指南、测试策略、配置、数据保留、运维及数据迁移回滚规范。（2026-08-01）
- 建立重要架构决策记录目录。（2026-08-01）
- 完成 DocMind 后端与前端、ResearchMind 后端的 M0 Monorepo 结构迁移，并保留可审查的来源提交与 Git 历史。（2026-08-02）
- 建立 M1 统一身份、服务认证与敏感数据外发策略 ADR 候选，进入 M1 规范与验收准备。（2026-08-02）
- 为 Knowledge 既有用户增加稳定 Platform User UUID，并将 Research Task 用户归属改为无跨库外键的 UUID。（2026-08-02）
- 为 Knowledge 入库流水线新增确定性 Clean 数据清洗阶段（页号/页眉页脚去噪、空白/空行规整、损坏 Unicode 修复），位于 Parse 与 Chunk 之间，由 `CLEAN_ENABLED` 与三个逐项开关控制，仅影响新入库内容；实现与单测见 `app/rag/cleaner.py`、`tests/unit/rag/test_cleaner.py`。（2026-08-02）

### Updated

- 完成 M4 External OpenAPI 与 v1 Provider 收敛：Chat v1 请求字段统一为 `question`，不接受 `message`；Research v1 创建请求保持嵌套 `requirements`，预算由服务端推导且拒绝客户端预算摘要；全部 `/api/v1/*` 成功响应直接返回资源/分页对象，错误返回 API.md §4 `error` 信封；正式报告以 `GET /api/v1/reports/{report_id}` 为唯一目标读取接口，Research Task DTO 在报告发布后返回 `report_id`，`GET /api/v1/research/tasks/{task_id}/report` 仅保留为带废弃调用量观测的迁移兼容入口。`docs/openapi/evidsight-v1.yaml` 从 9 条 KB/Document/Conversation 路径扩展到 29 条 Auth/Knowledge/Chat/Research/Evidence/Report 路径及两套 SSE `data` Schema；Knowledge/Research 双 FastAPI App 新增路由清单 ↔ OpenAPI ↔ Provider 测试覆盖的双向门禁。v1 Provider 已移除 `code/message/data` 成功信封，Research 发布任务查询补充 `report_id`，Chat 来源投影只暴露稳定 `document_uuid/segment_id`。M4 恢复「进行中」；切片 4 在途结果保留但不纳入本次后端前置提交，尚待 Consumer 适配、来源卡片到切片抽屉的实时鉴权联动和视觉验收。验证：OpenAPI 静态校验通过；Knowledge 受影响套件 161 项、Research v1/Evidence/Report 34 项、Research 路由覆盖 3 项全部通过；Python mypy Knowledge 213、共享 24、Research 242 个源文件零错误，Ruff 与 `git diff --check` 通过。ADR 检查 1—8：否（执行未发布 v1 draft 的负责人裁决，不改变服务边界、权限模型或数据生命周期）。（2026-08-09）
- 重新校正 M4 全平台外部契约门禁（纯文档与事实状态修正）：确认 commit `dc92313` 的 External OpenAPI 与三方一致测试只覆盖 Knowledge Base / Document / Conversation，不能证明 Auth、Chat、Research、Evidence、Report 及两套 SSE 字段契约已稳定；ROADMAP 将 M4 重新标为「受阻」，保留已完成切片 3B，切片 4 在途结果不作为完成证据，暂停切片 5 及后续 API Consumer 生产实现。API.md 明确 OpenAPI 仅对已纳入路径具有字段权威性，未覆盖 DTO 的最小基线不能替代正式 Schema，并登记 Chat `message/question`、Research 扁平/嵌套创建、错误信封、双 Report DTO 四类待裁决冲突；TESTING 将三方一致门禁扩大为 Knowledge/Research 双 FastAPI App 的全部浏览器外部 `/api/v1/*` 与 SSE `data` Schema，不允许前缀白名单局部通过。ADR 检查 1–8：否（纠正门禁覆盖与事实状态，不选择字段方案、不改变服务边界、权限或数据生命周期；具体公共契约裁决另行执行）。（2026-08-09）
- 校正 M2/M4 里程碑状态与 Provider 就绪门禁（纯文档变更）：反向审计确认 M2 原退出门禁只覆盖 Knowledge Pipeline 与 Internal Retrieval，未把「必须完成的 Knowledge 外部 API」转化为可执行门禁；M4 进入核验只补查 Evidence/Report 与文档 location，错误宣称全部外部 API 已稳定。ROADMAP 将 M2 由「已完成」重新标为「进行中」，补入 Knowledge Base、Document、Conversation v1、统一可见 KB 列表/范围筛选/名称搜索、External OpenAPI 与路由清单/Provider 测试门禁；M4 由「进行中」标为「受阻」，保留已完成的 React 工程、Auth、Shell 与 Workbench，暂停依赖缺失 Provider 的知识库、文档、会话切片。API.md 记录三类 Knowledge 外部 API 的当前 legacy 实现、目标态、Consumer、观测与退出条件，并补齐 FRONTEND §5.4 已要求但 §6.2 表遗漏的 additive v1 分块列表目标路由；README 与 DEVELOPMENT 同步真实阶段。M3 的 Pipeline 门禁仍保持已完成，并明确其 v1 CRUD/canonical SSE 是在完成标记后收口。ADR 检查 1–8：否（状态校正不改变技术决策；分块列表是未发布 draft 中对既有 PRD/FRONTEND 行为和稳定 Segment ID 的 v1 投影，向后兼容且没有服务边界、权限、数据生命周期或核心机制选择）。验证：`git diff --check` 通过；文档相对链接扫描无缺失；ROADMAP 状态、进入条件与 Provider 清单一致性扫描通过。（2026-08-09）
- 修正 M4 Admin P0 验收与成本边界（负责人 2026-08-08 裁决“实现服从规范”）：FRONTEND §13.3 第 7 项改为分别验证 Pipeline Trace、审计日志和安全运行摘要，确保 Trace 不含正文、Prompt、凭据、隐藏推理或 Token 成本；P0 页面明确包含审计日志，完整成本与计费账本保留 P1，不得以安全运行摘要或虚假数据冒充已上线计费能力。纯规范修正，不制造业务 RED；已核对 FRONTEND §6.1 页面范围、§6.3 安全字段和 §13.3 E2E 边界一致。ADR 检查 1–8：否（消除既有 PRD/FRONTEND 范围冲突，不改变服务边界、权限、安全或数据生命周期）。（2026-08-08）
- 重构项目级文档分层，消除 `specs/` 中规范与操作指南混放：原 `specs/OPERATIONS.md` 拆为声明性权威来源 `specs/RELIABILITY.md`（服务目标、健康、容量、备份恢复门禁和故障语义）与执行入口 `guides/OPERATIONS.md`（启动、日检、备份、恢复、发布和故障操作）；`specs/TESTING.md` 只保留环境矩阵、验收场景、阈值与发布门禁，命令和发布记录模板移入命名可区分的 `guides/TEST_EXECUTION.md`；新增 `guides/README.md`，同步 `AGENTS.md`/`CLAUDE.md` 权威矩阵、文档中心、规范索引、ADR、PRD、ARCHITECTURE、ROADMAP、DEVELOPMENT 及模块引用。配置、数据保留和数据迁移继续保留在 `specs/`，因为它们定义实现必须遵守的键、生命周期、映射与回滚门禁。ADR 检查 1–8：否（只调整文档归属和导航，不改变产品行为、部署拓扑、信任边界、数据或恢复语义）。验证：`git diff --check` 通过；本次变更文档相对链接扫描无缺失；旧 `specs/OPERATIONS.md`、`guides/TESTING.md` 链接和失效 `TESTING.md` §6/§7 操作引用扫描无残留；`docker compose config --quiet` 通过。（2026-08-08）
- 同步已接受的 `ADR-011` 到部署权威规范与入口文档：`ARCHITECTURE.md` 将 v1.0 生产基线改为三台 2C2G 云节点的 Edge/Research、Data、Knowledge 数据岛并保留开发单机全栈 Compose，补齐 Knowledge Beat 单实例、私网、资源背压、故障与演进语义；`RELIABILITY.md` 与 `guides/OPERATIONS.md` 分别定义运行恢复要求和三节点操作方法；`CONFIGURATION.md` 定义开发/生产配置隔离、私网地址和节点最小 Secret 集合，不新增业务配置键；`TESTING.md` 增加三份 Compose 联合静态检查、网络暴露、节点中断和跨节点恢复验收；`ROADMAP.md` 将 M5 交付目标同步为三节点 2C2G；`DEVELOPMENT.md` 与 README 明确保留 Mac 根 Compose 全栈开发、生产资产尚待 M5 实现。纯文档变更，不制造业务 RED。验证：`git diff --check` 通过；本次文档相对链接目标扫描无缺失；旧单机生产基线与 `proposed` 状态扫描无残留；`docker compose config --quiet` 通过。三份生产 Compose 属 M5 待实现资产，本次未运行。（2026-08-08）
- Research API v1 收敛与旧路由退出机制（切片 8，对齐 API.md §8/§8.2/§15 与 ROADMAP 2026-08-05「Research API 路径迁移到 /api/v1/research」裁决，负责人 2026-08-08 授权批次③）。① **抽取公共 application service**：新建 `app/api/research_common.py`，从旧前缀处理器迁出 `build_task_snapshot`/`build_task_events_response`（SSE 流）/`publish_cancel_requested`，v1 与旧前缀薄适配器共用同一套快照/SSE/取消事件逻辑。② **补齐 v1 路由**（`app/api/research_v1.py`，对齐 API.md §8 表）：`GET /tasks`（列表）、`GET /tasks/{task_id}`（详情）、`POST /tasks/{task_id}/cancel`（202）、`POST /tasks/{task_id}/resume`（断点续跑，旧 `/retry` 语义等价）、`DELETE /tasks/{task_id}`（204）、`GET /tasks/{task_id}/events`（SSE）；并收敛旧前缀 `state`/`report` 为 `GET /tasks/{task_id}/state` 与 `GET /tasks/{task_id}/report`（§8.2 目标「全部研究命令与查询收敛到 /api/v1/research/*」；API.md §8 表随本次补充这两条路由）。③ **旧前缀薄适配器 + 调用量指标**：`app/api/research.py` 语义保持等价（创建/列表/详情/删除/取消/续跑/stream/state/report），复用共享辅助，每个路由入口记录 `researchmind_old_api_calls_total{route}`（新增 `app/metrics/registry.py` Counter + `app/metrics/emitters.py::emit_old_api_call`，API.md §15 废弃观测）。④ **Consumer 清单与退出门禁**：仓库内前端（`apps/web`）当前不调用 Research CRUD 路由，脚本仅 `scripts/smoke_compose.sh` 使用 `/api/research/health`（健康探针，独立于 CRUD）；**旧前缀 CRUD 路由删除延后** —— 按 API.md §15「废弃路由必须记录调用量；在观测窗口归零且 Consumer 回归通过后才能删除」，观测指标为旧前缀调用量，删除（含旧信封/DTO/Nginx 旧前缀规则）待窗口归零且负责人确认后另起清理事件，本切片不执行。⑤ 测试：新增 `tests/unit/api/test_research_v1.py` 14 项（v1 list/get/cancel/resume/delete/events/state/report 验收 + E2001/E2002/204/409/422 错误路径）；`test_metrics.py` 新增旧前缀调用量指标 1 项。验证：API 相关 unit 94 项全绿（79 既有 + 15 新增）；路由注册冒烟确认 v1 前缀 9 条研究路由 + evidence/report 路由全部注册、旧前缀 9 条路由保留；ruff check/format 通过。ADR 检查 1–8：否（实现已批准的 API.md §8 目标态路由收敛，不改变服务边界/权限模型/公共契约语义/数据生命周期；v1 新增路由为既有 §3.1 语义在 v1 前缀的对等落地，`state`/`report` 两条 v1 路由为 §8.2 明确收敛目标；旧前缀删除被 §15 观测门禁明确延后）。（2026-08-08）
- 删除废弃的 `PipelineOrchestrator`，阶段定义收敛到唯一权威来源（切片 7，行为不变清理；AgentRuntime 自切片 E 起已为生产调度路径，编排器仅被旧测试驱动，负责人 2026-08-08 授权批次③清理）。① 新建 `app/pipeline/definition.py` 作为阶段定义的**唯一权威来源**：`PHASE_ORDER`/`PHASE_LABELS`/`STEP_TYPE_TO_PHASE`/`PhaseFunc` 从 `app/services/pipeline_orchestrator.py` 迁入（不再各自维护副本）；`build_default_phase_handlers` 移入 `app/tools/registry.py`（删除对已删模块的延迟导入）。② 重定向 4 处生产 import：`agent/runtime.py`（`PHASE_ORDER`/`STEP_TYPE_TO_PHASE`，`_phase_label` 改复用 `PHASE_LABELS` 消除重复映射）、`tasks/research_task.py`、`services/research_service.py`（并删除其自维护的 `_STEP_TYPE_TO_PHASE` 副本，仅保留反向 `_PHASE_TO_STEP_TYPE`）、`tools/registry.py`。③ 删除 `app/services/pipeline_orchestrator.py` 与仅验旧编排器的测试：`tests/unit/services/test_pipeline_orchestrator.py`、`tests/integration/test_pipeline_retry.py`（explicit legacy 断点续跑测试）及其唯一引用方 `tests/integration/_retry_helpers.py`。④ 受影响全链路集成测试迁移到 AgentRuntime 驱动：`tests/integration/test_pipeline_full.py`/`test_pipeline_evaluation.py` 由 `PipelineOrchestrator.run()` 改为 `AgentRuntime` + `build_default_tool_registry()`（真实 Phase Handler），mock `app.agent.loop.chat_completion` 按当前 phase 返回对应 Tool Call，删除 Redis step-lock patch 与预置 planning step，synthesis mock 补 critical Claim 以满足 §10.2 发布门禁，并注入 `TAVILY_API_KEY` 使测试在本地自洽可跑（searcher §6.1 api_key 预检查为既有 E3102 门禁，测试全程 mock `_call_tavily` 不产生真实调用）。验证：全量 unit（非 slow，注入 `TAVILY_API_KEY` 以满足 searcher 既有 E3102 预检查基线）997 通过、1 跳过、0 失败；集成 6 项全绿（`test_agent_runtime_flag`/`test_pipeline_full`/`test_pipeline_evaluation`，后者迁移后本地可跑不再依赖 Redis）；契约 73 + 131 全绿；Architecture 16 passed / 1 failed（既有 `.opencode/package.json` 布局干扰，与历轮一致）；ruff check/format 通过。ADR 检查 1–8：否（纯行为不变清理：删除死代码、收敛常量到唯一权威、测试驱动迁移，不改变服务边界/公共契约/数据/安全/核心机制）。**注**：RESEARCH_PIPELINE.md 的「Research Orchestrator」为组件角色概念（由 AgentRuntime 承担），非已删类名，保留。（2026-08-08）
- 收敛 Report 发布为 Revision 单写、双 API 同源读取，并落地完整度真实口径（切片 4-6，让实现重新符合已接受的 ADR-009 与 RESEARCH_PIPELINE §5.1/§10/§11/DATABASE.md §7/§12，负责人 2026-08-08 授权同步修订规范）。④ **切片 4 Report Revision 单写**（§11）：renderer 不再写 task 级迁移态 sections（删 `_persist_sections`/`_load_recent_report_sections`），`publish_report` 直接据渲染 DTO 创建 revision 专属 sections 并在同一事务同步写 `section_evidence`（引用经 `index_to_evidence_id` 解析、按 §9 门禁 1 闭合到当前 Task）；旧 API `get_report` 与 v1 读取 API 同源（reports→current_revision_id→revision sections），无 revision 时按「报告尚未生成」失败；`_create_direct_answer_task` 改走目标态 reports/revision；AC-001 脚本改读 revision sections。⑤ **切片 5 存量报告迁移**（DATABASE.md §12-4）：新增 `scripts/migrate_legacy_report_sections.py` + `app/services/legacy_report_migration.py`，为有 task 级迁移态 sections 且无 reports 根的可迁移终态任务幂等创建 Report + published Revision 1，把 sections 归入 Revision（保留 section_evidence、不重建不复制正文），非终态任务保留待渲染发布，dry-run 只扫描，校验无 `revision_id IS NULL` 残留。⑥ **切片 6 完整度真实口径**（§5.1/§10.1/§10.2）：Planning 输出稳定结构 `questions[{question_id, text, required, planned_channels}]`（LLM 未输出时按 sub_questions + source_strategy 派生，required 全 true）；`evidence_items` 新增 `question_id` 列（迁移 `i0j1k2l3m4n`），reranker 持久化时写入；`publish_report` 用 planning questions + evidence question_id 计算真实 question_coverage/channel_success 分子分母，零 required 子问题或零 critical Claim 发布失败（§10.2）；Revision 摘要持久化三分项 `{numerator, denominator, ratio}` + score + rule_version；TaskStateResolver 只接受 published Revision 摘要（已确认）。⑦ 测试：新增 `test_revision_single_write.py`（切片 4 验收 4 项）、`test_legacy_report_migration.py`（切片 5 迁移 5 项）、`test_completeness_real.py`（切片 6 验收 8 项）；适配 renderer/target_publish/duplicates/downstream/research_service/API 测试（task 级 sections 断言迁到 revision sections、graph 补 critical claim、direct_answer 目标态 seed）；删 `_compute_channel_success` 近似。验证：全量 unit（非 slow）1040 通过、1 跳过、1 failed（`test_llm` 本机 .env 覆盖 `LLM_FLASH_MODEL` 的既有环境依赖，与 clean HEAD 一致）；契约 73 + 131 全绿；Architecture 16 passed / 1 failed（既有 `.opencode/package.json` 布局干扰）；ruff check/format 通过；**真实 MySQL upgrade/downgrade/再 upgrade 往返验证通过**（空库 `research_migration_test2`：`h6i7j8k9l0m` 新增 redelivery_count、`i0j1k2l3m4n` 新增 evidence_items.question_id，downgrade `g5h6i7j8k9m` 两列删除、再 upgrade head 恢复；容器内 alembic 报 aiomysql "Event loop is closed" 为迁移完成后 cleanup 阶段无害噪音，迁移结果经数据库状态核验；**容器镜像为旧代码快照，生产部署需先重建 research 镜像**）。ADR 检查 1–8：否（让实现重新符合已接受的 ADR-009 与 RESEARCH_PIPELINE §5.1/§10/§11/DATABASE.md §7/§12；存量迁移遵循 DATA_MIGRATION_AND_ROLLBACK.md，不改变服务边界/公共契约兼容性/安全/数据生命周期；question_id 为受控新增列）。（2026-08-08）
- 收敛 Research 任务生命周期为 MySQL 单一事实源 + 周期 Recovery Scanner（切片 1-3，让实现重新符合已接受的 ADR-008「MySQL 为任务生命周期唯一权威」，负责人 2026-08-08 授权同步修订规范）。① 规范补充（先于测试/实现）：RESEARCH_PIPELINE §13.5 明确终态纪律（Recovery Scanner/API watcher/Celery 顶层异常一律不得直接写终态，`emergency_fail` 仅限无法进入 Resolver 的极端情况且需显式分类，错误码 E3999）、新增 §13.6「Pending 重投递与受控失败」（阈值 `PENDING_REDELIVERY_THRESHOLD_SECONDS`、最大次数 `PENDING_REDELIVERY_MAX_RETRIES`、超限创建 `planning` failed Step 错误码 **E3118** 由 Resolver 推导 failed）、§17.3 验收新增 19/20；DATABASE.md §5.1 恢复组新增 `redelivery_count`；CONFIGURATION.md 正式登记 `RESEARCH_TASK_LEASE_TTL_SECONDS`/`RESEARCH_TASK_LEASE_RENEW_INTERVAL`/`RESEARCH_RECOVERY_SCAN_INTERVAL_SECONDS`/`PENDING_REDELIVERY_THRESHOLD_SECONDS`/`PENDING_REDELIVERY_MAX_RETRIES`（自规划键移入，约束续租周期<租约/2、扫描间隔≤租约）并同步 `.env.example`。② 实现：`task_lifecycle.py` 的 `TaskLockHandle` → 纯 DB `TaskLeaseHandle`（删除 Redis acquire/release/refresh，只保留 DB 续租/释放；续租失败置 `lease_lost=True`，后续 Provider 调用与提交立即停止；`release()` 仅 owner 匹配清除，旧 Worker finally 不释放新 Worker 租约）；`start_research_task` 将 pending→running 与 DB lease 领取合并为**单条条件更新**（WHERE status IN pending|running、未取消、租约空/过期；SET status=running、started_at、lease_owner、lease_expires_at、lease_generation+1），领取失败立即返回 False 不执行业务步骤，删除「获取锁失败强制删除别人锁」路径；`main.py` 删除 `_worker_lock_missing_since`/`_mark_task_worker_timeout`/`_mark_task_pending_timeout`/`_check_worker_timeouts`/`_run_worker_timeout_watcher` 及 Redis 锁 import（不再因 Redis Key 缺失直接写 failed E3112/E3113），lifespan 不再启动 watcher；`AgentRuntime._execute_tool` 在 Provider 调用前检查 `lease_lost`；`_emergency_fail`/`emergency_fail_task` 新增显式 `failure_classification`（仅极端情况，E3999）；`ResearchTask` 新增 `redelivery_count`（迁移 `h6i7j8k9l0m`，head 由 `g5h6i7j8k9m` 前移）。③ 周期 Recovery Scanner（切片 3）：`recovery.py` 改为纯 DB lease 扫描（删除 `check_lock` 参数与 Redis 判断），Scanner 条件领取把 `lease_expires_at` 推至未来阻止并发 Scanner 双领取（两 Scanner 并发只恢复一次），清除旧 owner、递增 `recovery_count`，提交后投递 `research.execute`，投递失败任务保持可再次被扫描发现；新增 pending 重投递 `_handle_stale_pending`/`_create_pending_failure_fact`（超阈值无有效租约 → 递增 `redelivery_count` 重投；超上限 → 创建 `planning` failed Step E3118，由 `TaskStateResolver` 推导 `failed`，扫描器不直接写终态，E3118 已入 Resolver FATAL 集合）；新增 Celery Beat 任务 `app.tasks.periodic.recover_expired_research_tasks`（注册到 `beat_schedule`，调度间隔 `RESEARCH_RECOVERY_SCAN_INTERVAL_SECONDS`）；`config.py` 新增 `RESEARCH_TASK_LEASE_RENEW_INTERVAL`/`RESEARCH_RECOVERY_SCAN_INTERVAL_SECONDS`/`PENDING_REDELIVERY_*` 及 `model_validator` 校验（续租<租约/2、扫描间隔≤租约，fail-fast）；API 启动、Worker 就绪（`celery_app.py`）、周期 Beat、手动治理（AC-004 脚本）统一调用同一 `recover_stale_tasks()`。④ 测试：新增 `tests/unit/services/test_lifecycle_conflicts.py` 9 项（双 Worker 竞争/Redis 不可用 DB lease 照常/有效租约不被 Scanner 接管/过期后新 generation 接管旧 generation 不可提交/旧 Worker finally 不释放新租约/watchdog 不绕过 Resolver 写终态 + 扩展），其中「Redis 不可用仍能领取」「main 无直接写终态 watchdog」「过期 lease 恢复不依赖 Redis」三项当前实现下正确 RED、其余为既有语义回归锚点，切片 2/3 后全部 GREEN；重写 `test_task_lease_wiring.py`/`test_task_lifecycle.py`/`test_recovery_lease.py`/`test_startup_recovery.py`（移除 fake_locks 与 check_lock/Redis patch，新增 pending 重投递、Scanner 条件领取互斥、两轮扫描幂等重投等验收）；删除 `test_worker_timeout.py`；新增 `test_config_validation.py`（租约间隔约束 fail-fast 3 项）、`test_periodic.py` 周期恢复任务 3 项；适配 `test_runtime.py`/`test_runtime_lease.py`/`test_runtime_budget.py`/`test_agent_runtime_flag.py`（`_lease_handle` 命名、移除已删除 Redis 锁函数的 patch）。验证：本地全量 unit（非 slow，source 根 `.env`）1017 通过、1 跳过、1 failed（`test_llm` 为本机根 `.env` 覆盖 `LLM_MODEL` 的既有环境依赖，与 clean HEAD 一致）、契约 73 项全绿、Architecture 16 项全绿（1 项既有前端布局失败，非本次引入）；`test_pipeline_full` 失败为旧编排器 Redis task lock 未 mock 的既有环境依赖（走 deprecated 路径，切片 7 删除）。ADR 检查 1–8：否（让实现重新符合已接受的 ADR-008 与 RESEARCH_PIPELINE §13/DATABASE.md §8；pending 重投递与恢复配置为按授权补充的规范缺口，不改变服务边界/公共契约兼容性/安全/数据生命周期；E3118 为受控新增错误码）。（2026-08-08）
- 标记 M3 为已完成并勾选全部退出门禁（落实 ROADMAP §M3 退出门禁 8 项；纯文档变更，AGENTS.md 例外条款——不改变服务边界/公共契约/权限模型/数据生命周期，ADR 检查 1–8：否）。逐项核对在 docker 全栈 research-api 容器内执行（`--rm` 临时容器挂载工作区，补装 `aiosqlite`，测试连 SQLite 内存引擎零外部依赖）：① knowledge 只使用有权访问的内部知识 31 passed（`test_source_strategy`/`TestRunPipelineStrategyGuard`/`test_knowledge_search`/`test_rerank_internal`）；② web 带 URL/获取时间 Evidence 7 passed（`test_rerank_fetched_at`/`test_ac_metrics` web 分支）；③ hybrid 报告区分来源 7 passed（`test_downstream_internal`/`TestRerankHybridInternal`/`TestHybridStrategySearch`）；④ 私有内容不进 Web 搜索词通过（`test_knowledge_search` 两用例）；⑤ 冲突来源不合成无条件确定结论 5 passed（`test_synthesis_conflict_consensus`）；⑥ 取消后 Worker 不继续产生业务结果 42 passed（`test_cancel_request`/`test_task_lease`/`test_runtime_lease` 取消路径）；⑦ 中断后从安全断点继续不重复 38 passed（`test_recovery_lease`/`test_task_lease_wiring`/`test_runtime_lease`/`test_trace_crash_recovery`/`test_startup_recovery`）；⑧ AC-001/003/004/010 验证入口已建立（脚本门槛 AC-001 ≥0.90、AC-003 ≥0.95、AC-004 ≥0.95、AC-010 100%，`test_ac_metrics` 23 passed）。回归基线：research 全量 unit（非 slow）1084 passed / 1 skipped / 1 failed（`test_llm` 为本机根 `.env` 覆盖 `LLM_FLASH_MODEL` 的环境依赖，与历轮 clean HEAD 失败集一致）、契约 204 passed（research 73 + packages/contracts 131）、Architecture 16 passed / 1 failed（`test_no_second_runnable_frontend_exists` 为 git-ignored `.opencode/package.json` 干扰布局断言的既有失败，stash 前后一致非本次引入）。真实库冒烟：AC-010 追溯率 100%（10 条 web evidence）；AC-001 对事故样本任务 `53d881ec`（2026-08-07 验证事故中 section_evidence 被级联删除）fail-closed 报 0% 不伪造、AC-003 因评估集 topic 无匹配任务分母 0 fail-closed 报未通过，均为脚本设计行为。S5/S7 已确认排期至后续切片不构成 M3 遗留缺口。同步更新 ROADMAP（M3 状态→已完成、退出门禁勾选、M3 完成记录）与 DEVELOPMENT.md（当前阶段→M4）。（2026-08-08）
- 落实 2026-08-06 文档裁决三项（① 配置键、② 错误码、③ 文档状态）：
  ① 配置键命名（豁免 ADR，方案 B）：`CONFIGURATION.md` §2/§3.1 注册表改为只登记实现真实读取的键——删除虚构键 `EVIDSIGHT_PLATFORM_JWT_ALGORITHM`/`ACTIVE_KID`/`PRIVATE_KEY_FILE`/`PUBLIC_KEYS_FILE`/`ACCESS_TOKEN_TTL_SECONDS`/`REFRESH_TOKEN_TTL_SECONDS`、`EVIDSIGHT_KNOWLEDGE_DATABASE_URL`/`REDIS_URL`/`UPLOAD_DIR`/`CHROMA_DIR`/`CLEAN_*`、`EVIDSIGHT_RESEARCH_*` 租约键、`EVIDSIGHT_PROVIDER_*` 等未实现键，按实际键名登记（`JWT_SECRET_KEY`/`REFRESH_TOKEN_SECRET_KEY`/`JWT_ALGORITHM`/`ACCESS_TOKEN_EXPIRE_MINUTES`/`REFRESH_TOKEN_EXPIRE_DAYS`/`MYSQL_*`/`REDIS_URL`/`CELERY_*`/`UPLOAD_DIR`/`UPLOAD_MAX_SIZE`/`ALLOWED_EXTENSIONS`/`CHROMA_PERSIST_DIR`/`CLEAN_*`/`LLM_*`/`EMBEDDING_*`/`RERANK_*`/`TAVILY_*`），并新增「规划键（未实现，不登记）」说明；`.env.example` 补齐 Service JWT 与内部检索必填键。
  ② 错误码双轨（豁免 ADR，先 B 后 A）：`API.md` §8.3 固化「内部新码 ↔ 迁移期 E 码」映射表（E1010/E9002/E3114–E3117/E2009）与退出条件——全平台信封统一落地 API.md §4 时随迁新码并删除旧 E 码。
  ③ 文档状态（ADR 未命中）：`deleting` 对外暴露，`API.md` §3.1/§6.2 与 `FRONTEND.md` §5.4 补 6 值枚举。
  纯文档变更，已执行格式与链接检查。（2026-08-06）
- 文档治理与权威规范正文过程记录收敛（纯文档变更）：修订 `docs/decisions/README.md` 与 `docs/guides/DOCUMENT_GOVERNANCE.md`（§2 Changelog 职责、§5.3），允许 ADR 检查/裁决/迁移态记录集中写入 CHANGELOG，权威规范只保留当前事实与 CHANGELOG 指针；`docs/specs/API.md` §8 移除内嵌裁决/验证记录并改为引用；`docs/plans/ROADMAP.md` M1/M2/M3 内嵌验证记录压缩为状态+CHANGELOG 引用；同步 README 项目状态、DEVELOPMENT 当前阶段与前端技术栈表述、FRONTEND 文档日期、AGENT.md→AGENTS.md 文件名引用。已执行格式与链接检查。（2026-08-06）
- 更新 API.md §8（Research Task API）：新增 §8.1 创建语义（Idempotency-Key 必填、同 Key 同指纹重放 `idempotent_replayed=true`、不同指纹 409 `E2009`、KB 创建时结构校验 + Internal Retrieval 实时鉴权）与 §8.2 信封迁移态记录（当前态 `{"code","message","data"}` 信封、目标态 API.md §4 `{"error":{...}}`、迁移步骤/Consumer/观测/退出条件，对齐文档治理 §5.3）；并记录 ADR 检查 1–8：否与负责人 2026-08-05 三项裁决（信封沿用既有、KB 实时鉴权、新增 idempotent_replayed）。纯文档变更，已执行格式与引用一致性检查。（2026-08-05）
- 批准并落地 M2 审查 I2 规范变更：`EVIDSIGHT_PLATFORM_AUTH_ALLOWED_ORIGINS` 生产环境必填（为空拒绝启动，fail-fast），`CONFIGURATION.md` 同步该必填语义；开发环境维持同站处理不阻断本地迭代。验证：`validate_production_config` 生产空 origins 返回错误、配置齐全返回空。（2026-08-05）
- M1 收尾并标记完成：六条退出门禁逐条核对证据齐备（双侧 JWT Claims/过期/禁用语义测试一致、禁用用户全链路拒绝、服务间伪造拒绝、KB 权限矩阵、Contract Provider/Consumer 测试、敏感信息不进前端响应或普通日志），ROADMAP 将 M1 状态由"进行中"更新为"已完成"并勾选全部门禁清单，DEVELOPMENT.md 当前阶段同步；Retrieval/Evidence Contract 与 IA-006—IA-009 依赖 Internal Retrieval，明确划入 M2/M3。（2026-08-04）
- 固化 M1 Refresh Token Cookie/CSRF 安全收口规范：浏览器目标态使用 HttpOnly Refresh Cookie + double-submit CSRF，Refresh/Logout 先校验 CSRF/Origin，旧 body `refresh_token` 仅作为迁移期兼容入口。（2026-08-03）
- 固化外部 User DTO UUID 与遗留 Auth 接口退出规则：`/api/v1/auth/*` 的 User DTO `id` 一律为 Platform User UUID，旧 `/api/auth/*` 与旧 `id=int` UserResponse 仅作为 M1 迁移期兼容并需调用量观测后删除。（2026-08-03）
- 实现 IA-013/IA-014 事件②：Access Token 移除 `username` 等可派生展示 Claim，前端身份权威来源改为 `GET /api/v1/auth/me`（`UserSummary`，`id` 为 Platform User UUID 字符串，`username`/`role`/`status` 来自数据库当前状态）；登录、重载、Refresh 均经 `/me` 重建身份，`isLoggedIn` 在 `/me` 完成前保持未就绪，客户端不再解析 JWT Claim 或恢复持久化 `user`。（2026-08-03）
- 落地事件①外部 User DTO UUID 化（IA-017）：`/api/v1/auth/register` 返回 `UserSummary`（`id` 为 Platform User UUID），旧 `/api/auth/register` 保留 `UserResponse(id=int)` 迁移期兼容；admin 用户 DTO 与 `{user_id}` 路由参数改 Platform User UUID；B 类外部响应用户引用全部移除内部 `users.id`（`KnowledgeBaseResponse`/`PublicKnowledgeBaseResponse` 用 `owner`，`AdminKBItem`/`ConversationResponse`/`TraceListItem`/`TraceDetailResponse` 用 `owner_user_id`，`AdminDocItem` 用 `owner_id`）；前端 KnowledgeDetail isOwner、admin TraceList/TraceDetail/KnowledgeList 视图同步对齐 `owner`/`owner_user_id` 展示与跳转。（2026-08-03）
- 固化 M1 Internal Identity Status 公共契约：Research 创建长期任务前通过独立 Service JWT 向 Knowledge 实时复核用户状态，身份事实源不可用时失败关闭。（2026-08-03）
- 实现 IA-004 Refresh Token 重放响应：已轮换 Token 再次出现时原子撤销所属 Family，持久化不含 Token 的身份安全审计事件并返回 `E5009`。（2026-08-02）
- 实现 IA-003/IA-011 Refresh Token 原子轮换：Knowledge 登录创建 Platform User UUID 归属的 Token Family，刷新锁定旧 Token、记录唯一后继并阻止并发双花。（2026-08-02）
- 移除 Research 本地注册、登录、刷新与密码管理能力及 `users`、`refresh_tokens` 表；Research 仅验证 Knowledge 签发的 Access Token，并以外部 Platform User UUID 保存任务归属。（2026-08-02）
- 实现 IA-001/IA-002 首个纵向切片：Access Token 补齐 issuer、双 Audience、UUID subject、类型、JWT ID 和时间 Claim，Knowledge 与 Research 分别校验自身 Audience。（2026-08-02）
- 明确统一 Access Token 使用双 Audience 数组，Knowledge 与 Research 分别配置并验证自身 Audience；补充 IA-001/IA-002 可执行验收映射。（2026-08-02）
- 接受 ADR-005，解除 M1 验收测试设计门禁并进入首个纵向切片确认阶段。（2026-08-02）
- 建立 ADR 二元触发检查、负责人明确裁决、豁免留痕和既有决策替代门禁，并将其接入 SDD 入口。（2026-08-02）
- 统一 M0 已完成、M1 准备中的阶段状态；根据负责人裁决移除已删除 `docs/migration/` 过程记录的门禁和失效引用。（2026-08-02）
- 将项目级文档分为 `specs/`、`guides/`、`plans/`、`decisions/` 与迁移证据层，并新增文档中心和权威规范索引；模块专项文档继续跟随模块维护。（2026-08-01）
- 明确 v1.0 Chat 只绑定单个知识库；前端保留可演进选择器，但多选不可执行并提示“多知识库问答规划中”。（2026-08-01）
- 多 KB 能力仅用于 Research 通过权限感知的 Internal Retrieval 检索；多 KB Chat 延后至 v1.x。（2026-08-01）
- 统一 Research 七阶段顺序为 Planning → Searching → Fetching → Reranking → Synthesizing → Evidence Graph Build → Rendering。（2026-08-01）
- Internal Evidence 身份增加稳定 `document_version_id`，用于重处理后的历史来源追溯。（2026-08-01）
- 明确 M0 保留 Vue 来源迁移基线，M4 按前端专项规范交付唯一 React + TypeScript Web。（2026-08-01）
- 将 LLM、Embedding、Rerank 与 Tavily 的 Base URL 和模型配置从根目录 `.env` 透传到对应服务容器，并清理未接线的示例变量。（2026-08-02）
- Knowledge 离线检索评估 `eval_retrieval.py` 在指标未达标时以非零码退出，使 Rec@5 成为可重复 fail-fast 门禁（用于清洗前后度量）。（2026-08-02）

### Fixed

- 纠偏 2 工作台快速开始卡片动作文案右移（负责人复核后指示，与空态按钮右对齐同思路）：「进入问答 →」「定义范围 →」此前位于 Action Card 标题文案下方，改为位于卡片右下侧、与标题文案（向企业知识提问/发起新的研究任务）同一水平线；卡片改为说明居左（`.launcher-card__copy` 包裹 kicker 与标题）、动作居右（`.launcher-card__action`，卡片 `align-items: flex-end` 底对齐）的 flex 行布局，首个版本用 `align-items: center` 使动作垂直居中于 kicker 与标题之间，负责人指正后改底对齐。FRONTEND §5.2 与 UIDESIGN §7.2 同步该布局。新增单测 1 项（断言动作文案与标题文案分离、位于右侧动作元素）。工作台 6 张视觉基线重新生成。验证：RED 确认（`.launcher-card__copy` 结构缺失致新测试失败）；vitest 全量 **126 项**、ESLint、Prettier、TypeScript/Vite build、Design Token（40 Token/49 文件）与视觉基线门禁（20 页/40 截图）、Playwright 全量 **15 项**全部通过；`git diff --check` 通过。ADR 检查 1–8：否（落实既有 UIDESIGN §7.2 与负责人指示，纯前端卡片布局，不改变服务边界、公共契约、权限模型或数据生命周期）。（2026-08-10）
- 纠偏 2 工作台空态按钮布局修正（负责人复核后指示）：`RECENT RESEARCH` 无数据时「开始研究」按钮此前位于空态说明句下方，改为位于「查看全部」下方右侧、与「明确研究范围，系统会持续保存任务事实。」说明句处于同一水平线（说明居左、动作居右）。`EmptyState` 新增 `actionAlign` 变体：`"below"`（默认，动作位于说明文案下方）与 `"beside"`（动作与说明文案同一行、右对齐，新增 `.empty-state--beside`/`.empty-state__action-row` 样式）；工作台最近研究空态改用 `beside`，其余 EmptyState 用法不变。FRONTEND §5.2 与 UIDESIGN §7.2 同步该布局（动作主按钮位于区块右侧、"查看全部"下方）。新增单测 1 项（断言按钮与说明句处于同一行容器）。视觉基线 `workbench-empty-light/dark` 重新生成（旧 dist 误用导致首次基线未生效，`vite preview` 服务旧构建，重建后再生成）。验证：RED 确认（beside 结构缺失致新测试失败）；vitest 全量 **125 项**、ESLint、Prettier、TypeScript/Vite build、Design Token（40 Token/49 文件）与视觉基线门禁（20 页/40 截图）、Playwright 全量 **15 项**（workbench 10 + landing 5）全部通过；`git diff --check` 通过。ADR 检查 1–8：否（落实既有 UIDESIGN §7.2 与负责人指示的布局，纯前端样式与组件变体，不改变服务边界、公共契约、权限模型或数据生命周期）。（2026-08-10）
- 命令面板空态布局修正（负责人复核后指示）：输入无匹配时「无匹配命令」此前为流内块元素，渲染后撑高 `.command-palette`（32px 输入框 + 空态文本），配合 `.context-bar__actions` 的 `align-items: center` 把输入框顶出顶栏上方。改为与命令列表同款绝对定位浮层（`position: absolute; top: calc(100% + 8px); width: 100%` + `--es-border-default`/`--es-radius-major`/`--es-surface`/`--es-shadow-popover`），不参与流内高度，仅作不可跳转提示。新增 e2e 回归「输入无匹配展示无匹配命令且不顶起输入框」（断言输入框 y 坐标不变）+ 空态卡片视觉基线。验证：vitest 全量 **124 项**、ESLint、Prettier、TypeScript/Vite build、Design Token（40 Token/49 文件）与视觉基线门禁、Playwright 全量 **15 项**（workbench 10 + landing 5）全部通过。（2026-08-10）
- 纠偏 2 工作台复核修正二（负责人复核后指示）：运行任务 Chip 常显与空态行动入口按钮化。① 运行任务 Chip（FRONTEND §4.3）由「仅运行任务 >0 时渲染为链接」改为常驻按钮：有运行任务显示「N 项研究进行中」（青色信号点），无任务显示「暂无任务进行」（中性点，`running-chip--idle`）；点击弹出具名确认框「是否跳转到研究任务？」，确认后 `navigate('/research')`，取消停留当前页；AppShell 移除 Chip 对运行数量的隐藏逻辑，新增 `chipConfirmOpen` 状态与 `ConfirmDialog`，单元测试 2 项改写（运行态 Chip 常显数量 + 确认跳转；空态 Chip 显示「暂无任务进行」）。② 空态行动入口按钮化：EmptyState 的「开始研究/创建知识库/开始问答」此前是裸 `<Link>`（浏览器默认紫字无按钮感），统一加 `className="btn btn--primary"` 对齐既有 KnowledgeBaseListPage「新建知识库」按钮约定，`.btn` 补 `text-decoration: none` 支持链接按钮。③ 工作台与命令面板相关视觉基线重新生成。验证：RED 确认（Chip 空态与确认跳转 2 项新测试失败）；vitest 全量 **124 项**、ESLint、Prettier、TypeScript/Vite build、Design Token（40 Token/49 文件）与视觉基线门禁、Playwright 全量 **13 项**全部通过。ADR 检查 1–8：否（落实既有 FRONTEND §4.3/§10 与 UIDESIGN §6.1，纯前端行为与样式修正，不改变服务边界、公共契约、权限模型或数据生命周期）。（2026-08-10）
- 抽取共享 Route Surface 基底容器并统一尺寸（负责人指示：白色大容器将是后续所有 Route Surface 页面的公共基底，需统一大小）。① 新增共享 `.route-surface` 容器类：`max-width: 1260px`（对齐原型 `route-view`）+ 侧边 padding `40px`（内容宽 1180px，对齐 UIDESIGN §5.2「工作台最大约 1180px」）+ `--es-surface`/1px `--es-border-default`/`--es-radius-major`，不使用阴影；UIDESIGN §5.5 增加统一容器实现说明，后续页面根元素加 `route-surface` 类接入，不再复制私有容器样式。② 工作台根元素由 `.workbench` 改为 `.workbench route-surface`，移除 `.workbench` 私有的 1180px/28px-32px 容器样式（其职责移交共享类）；`.app-shell__content` 外层 padding 由 `24px` 收至 `16px`，容器贴边对齐原型（实测容器宽约 1032px，右缘距视口约 16px，四周保留浅灰 Canvas）。③ 工作台 6 张视觉基线重新生成。验证：vitest 全量 **119 项**、ESLint、Prettier、TypeScript/Vite build、Design Token（40 Token/48 文件）与视觉基线静态门禁、Playwright 全量 **11 项**全部通过；`git diff --check` 通过。ADR 检查 1–8：否（落实既有 UIDESIGN §5.5/§5.6 Surface 语法与原型 route-view，把工作台容器固化为共享基类，不改变服务边界、公共契约、权限模型或数据生命周期）。（2026-08-10）
- 纠偏 2 工作台视觉复核修正（负责人复核 02-workbench 与本地真实数据后指示）。① 「进行中的研究」卡片始终渲染：右栏上下文列不再在无运行任务时整卡隐藏，改为卡片内展示「暂无进行中的研究」占位文案与「开始研究」入口，`RECENT KNOWLEDGE` 固定在卡片下方不随运行状态上下浮动；WorkbenchPage 右栏改为无条件渲染 active-mission，新增工作台单测 1 项（无运行任务时卡片保留 + 占位 + 行动入口 + 知识库固定下方），FRONTEND §5.2 同步该行为。② 全局容器外层间距收紧：`.app-shell__content` padding 由 `36px` 收至 `24px`，Route Surface 容器贴边对齐原型（实测容器与 header/侧边栏间隙由 36px 降至约 24px，浅深一致），四周仍保留浅灰 Canvas（UIDESIGN §5.5）。③ 视觉验收基线同步：空态截图纳入 active-mission 占位卡片，工作台 6 张基线重新生成，e2e 空态断言改 `exact: true` 区分「开始研究」与「开始研究 →」。④ dev 环境 500 修复（环境操作）：`GET /api/v1/research/tasks` 因 `research_tasks.redelivery_count` 列缺失返回 500（pending 重投递功能迁移 `h6i7j8k9l0m` 未应用），在 research-api 容器执行 `alembic upgrade head` 后 DB 到达 head `i0j1k2l3m4n`、列补齐，真实数据恢复流入工作台。验证：RED 确认（无运行任务时 active-mission 缺失致新测试失败）；vitest 全量 **119 项**全绿（新增 1 项）、ESLint、Prettier、TypeScript/Vite build、Design Token（40 Token/48 文件）与视觉基线静态门禁、Playwright 全量 **11 项**（workbench 6 + landing 5）全部通过；像素采样确认浅色容器 `#ffffff`/Inset Panel `#f8fafb`/Canvas `#f3f4f6`、深色 `#0d111a`/`#151a27`/`#07090f`。ADR 检查 1–8：否（落实既有 UIDESIGN/FRONTEND，纯前端行为修正与视觉基线、dev 环境迁移补齐，不改变服务边界、公共契约、权限模型或数据生命周期）。（2026-08-10）
- 主题切换文档与已实现的两步骤确认流程对齐（纯文档变更，落实纠偏 2 已实现行为）。FRONTEND §3.3/§4.4/§14 与 UIDESIGN §4.1/§12 原先以「选择/切换立即生效」描述单步切换，与实现（账号菜单「主题选择」→ 居中选择卡片 →「切换主题？」确认卡叠加确认 → 顶部 Toast 约 2.6 秒自动消失）矛盾；且两步骤确认与 Toast 此前无权威视觉规格。现将两步骤确认流程与持久化写回 FRONTEND §4.4，新增 UIDESIGN §6.13 Toast 视觉规格，并在 §7.21 Overlay 矩阵登记主题选择卡片与主题切换确认卡，同步修改 4 处「立即生效」措辞；UIDESIGN §4.0 的 `--es-swatch-light/dark` 固定色板 Token 保持不变。按 AGENTS.md 纯文档例外不制造业务 RED；已执行 Markdown 格式与内部交叉引用检查。ADR 检查 1–8：否（文档与既有已批准实现对齐，不新增产品行为、路由、API、权限或交付范围）。（2026-08-09）
- 纠偏 2：AppShell 与工作台行为与结构修复（对齐 FRONTEND §3.3/§5.2/§4.3、UIDESIGN §5.2/§6.5/§7.1 与跟踪版原型 `02-workbench`，落实 ROADMAP「纠偏 2：AppShell 与工作台视觉 RED/GREEN」中行为与结构部分；明暗主题、空态/有数据/运行态截图等视觉 RED/GREEN 由负责人另行执行）。① **AppShell**：232px 固定侧栏 + 56px 顶栏与真实 BrandMark 保留；六项导航统一 18px 语义图标、行高不低于 44px，当前项同时具备背景、左侧标记与文字权重（不只换颜色，UIDESIGN §5.2）；顶栏由静态「证据工作区」改为真实 Breadcrumb「EVIDSIGHT / 当前页面」（按路由前缀映射）；运行任务 Chip 只显示真实运行中任务数量（`GET /api/v1/research/tasks?status=running` 的 total，0 时不渲染、不伪造运行状态）；搜索/命令入口未实现，不渲染可点击假按钮；账号区补齐头像（用户名首字符）、用户名、角色层级（成员/管理员）与展开状态，账号菜单 Trigger 增加 `aria-expanded` 与 `aria-controls`；管理入口仅对管理员显示；深浅主题只替换 Token，不改变布局。② **工作台**：标题改为用户化问候（`/me` 用户名 + 时段）+ 日期 + 更新时间；快速提问与深度研究合并进原型主要 Launcher 区，不再是两个孤立通用卡片；主列 + 右侧上下文列结构（工作台最大宽度约 1180px，1280px 下主次列不冲突）；RECENT RESEARCH 使用连续 Hairline 列表展示来源类型/状态/进度/继续入口；正在运行的任务显示「进入研究现场」，不重新创建任务；RECENT KNOWLEDGE 放入右侧紧凑列表；无研究数据保留「开始研究」空态、无知识库保留「创建知识库」空态；全部数据来自真实 API（`/api/v1/research/tasks` 与 `/api/v1/knowledge-bases`），不使用虚构 KPI、虚构任务或原型 Mock 数据填充页面。③ **契约补充（向后兼容）**：`ResearchTaskListItem` 增补 `source_strategy`（既有 `research_tasks.source_strategy` 列投影，knowledge/web/hybrid）与 `progress`（复用 `_build_progress` 同源口径，0–1），同步 External OpenAPI（可选字段，旧 Consumer 安全降级）与 Schema/Service 测试，使工作台可真实展示来源类型与进度。验证：后端 RED 确认（`ResearchTaskListItem` 缺 `source_strategy` 致新增用例失败）；research schemas/services/api **107 项** + External OpenAPI 路由一致性 **3 项**全绿、mypy 零错误、ruff check/format 通过、`check_openapi.py` 校验通过；前端 app-shell **6 项** + workbench **5 项**新增/重写全绿、Web 全量 **114 项**全绿、tsc/vite 构建/ESLint/Prettier/`check:design-tokens`（38 Token/47 文件）/`check:visual-baselines`（20 页/40 截图）全绿。既有失败记录：`services/research/tests/unit/api/test_sse.py::TestSseSnapshotStructure::test_快照包含所有必要字段` 断言 legacy `/api/research/{id}/state` 快照含 `topics`，该字段在 v1 收敛重构（commit `86344fd`）后已移出 `build_task_snapshot`，为存量漂移且与本纠偏无关（git 基线一致），未在本次修复。ADR 检查 1–8：否（落实既有 UIDESIGN/FRONTEND 规范与已跟踪原型；`source_strategy`/`progress` 为向后兼容可选字段投影，不改变服务边界、权限模型、公共契约语义、数据生命周期或生产运行机制）。（2026-08-09）
- 统一 Research v1 SSE 为 API.md §13 canonical 事件，同时保留旧 granular 兼容层（负责人 2026-08-08 裁决“实现服从规范”）。`GET /api/v1/research/tasks/{task_id}/events` 独立投影：`task.status.snapshot → snapshot`、`task.* → task.updated`、`phase.* → phase.updated`、`step.*|checkpoint.*|agent.* → step.updated`，原始安全 `data` 与持久事件 ID 保持不变；终态订阅发送 `snapshot` 后以 `stream.end` 关闭，实时订阅自然结束也发送 `stream.end`，且 `stream.end` 不代表任务成功。旧 `/api/research/{id}/stream` 继续输出 granular 事件并保留既有废弃调用量指标。验证：正确 RED 后进入 GREEN；Docker Compose Research canonical 投影与旧兼容层 12 项全绿；完整 `test_research_v1.py` 的 14 项在 fixture 初始化阶段被镜像缺少既有测试依赖 `aiosqlite` 阻断，未误报为业务失败。ADR 检查 1–8：否（只让 v1 Provider 符合既有目标契约，不改变任务事实、权限或持久化游标语义）。（2026-08-08）
- 修复 M4 进入评审阻断项 5 项（🔴1–🔴5）与规范冲突 1 项（🟡6），对齐 RESEARCH_PIPELINE §10.1/§10.2/§11/§13.1/§13.5、DATABASE.md §8/§12-4、API.md §8.3（ADR 检查 1–8：否——均为让实现重新符合既有权威规范，不改变服务边界/公共契约/权限模型/数据生命周期）。① **租约过期后旧 Worker 不得续租/提交/推导终态（🔴1，§13.1）**：`renew_task_lease` WHERE 增加 `lease_expires_at` 未过期条件（过期租约不可被旧 Worker 复活）；`is_step_commit_allowed`/`is_task_ownership_valid` 读取并校验 `lease_expires_at` 未过期，过期 Worker 的迟到提交与终态推导均被拒绝；`TaskLeaseHandle.renew_lease` 异常分支置 `lease_lost=True` 并停止续租协程（续租异常与返回 False 同权）。② **Recovery Scanner 只恢复一次（🔴2，§13.5）**：恢复成功后保留 scanner handoff 租约（`RECOVERY_SCANNER_WORKER_ID`，不再立即清空 owner/expiry），下一轮扫描条件（租约为空/已过期）不命中 → 不再重复投递；`claim_task_lease`/`start_research_task` 领取条件显式接受 scanner handoff 使新 Worker 立即接手；投递失败时清除 handoff 租约保证下轮可再发现。③ **E3114 业务失败经 Resolver 推导终态（🔴3，§13.5）**：E3114 加入 `FATAL_STEP_ERROR_CODES`（fail-closed 不可恢复）；`_enforce_strategy_dependency` 创建 `planning` failed Step 事实（E3114）交 `TaskStateResolver` 推导 failed，移除 `emergency_fail_task` 直写路径（后者仅限数据库损坏/Resolver 不可用，固定 E3999）。④ **报告发布硬门槛强制 + 真实口径唯一（🔴4，§10.1/§10.2/§11）**：门禁 3 强制每个 critical Claim 至少一条闭合的 supports Relation，否则发布失败；门禁 4 章节引用与 Claim 关系必须闭合到当前 Task 的 Evidence（§11 重跑引用闭包），未知/跨任务 evidence 一律发布失败而非静默忽略；移除 `_compute_real_coverage` 的 `total_steps`/`total_evidence` 近似口径，Planning 未产出 `questions` 结构时发布失败（§17.2-13）。⑤ **存量报告迁移显式提交 + 残留校验修复（🔴5，DATABASE.md §12-4）**：`scripts/migrate_legacy_report_sections.py` 非 dry-run 显式 `session.commit()`（否则退出会话写入回滚、脚本仍报告成功）；`legacy_report_migration` 残留校验改为对候选 task_id 集合判成员（原先字符串对元组列表判成员永不命中）。⑥ **规范冲突收敛（🟡6）**：RESEARCH_PIPELINE §10.1 迁移态近似声明改为「真实口径唯一」（移除近似口径，与 §17.2-13 一致），§13.1/§13.5 同步行为语义（过期租约不可复活、scanner handoff 租约只恢复一次）；`renderer._load_planning_questions`/`publish_report` docstring 同步。验证：RED 逐项确认（租约过期续租/提交/推导 4 项、两轮扫描二次投递、E3114 直写终态、发布门禁 4 项、脚本不提交/残留不命中）；research 全量 unit + 集成（非 slow，注入 `TAVILY_API_KEY` 满足 searcher 既有 E3102 预检查）**1034 通过、1 跳过、0 失败**；受影响套件（租约/恢复/E3114/renderer/发布/迁移）109 项全绿；ruff check/format 通过；`git diff --check` 通过。新增测试：`test_report_publish_gates.py`（门禁 3/4 + 真实口径 5 项）、租约过期校验 4 项、E3114 Step 事实 1 项、迁移提交/残留 3 项；修改既有固化错误行为的夹具（`test_task_lease_wiring`/`test_task_lifecycle` 过期续租种子、`test_recovery_lease` 两轮扫描改为只投一次、renderer/downstream/duplicates 补 Planning questions 真实口径）。（2026-08-08）
- 修复 M3 代码审查规范项 S1/S2/S3/S4/S6/S8/S9/S10（对齐 RESEARCH_PIPELINE/PRD 权威规范，负责人 2026-08-07 裁决：实现服从规范；ADR 检查 1–8：否——均不改变服务边界/公共契约/权限模型/数据生命周期，S5/S7 排期另议）。① **S1 预算停止终态完整度硬门槛**：`TaskStateResolver` 预算停止不再用证据计数 `max(5, ceil(max_sources*0.4))` 近似，改用已发布 `ReportRevision.evidence_completeness` 的 `score ≥ 0.70` 判定 partial/failed（§10.2 硬门槛含「存在通过结构校验的 Revision」：render 前停止无 Revision → failed E3103）；`AgentRuntime._load_published_completeness` 查询当前 Revision 摘要传入 Resolver。② **S2 总时限预算停止落库**：新增 `budget_service.mark_budget_stopped`，`_execute_tool` 检测总时限到期时写入 `budget_stopped_at` 并记录 `budget.stop` 事件，使 Resolver 按预算停止推导终态、报告披露「总时限」缺失（此前 deadline 分支只返回 False 不写列）。③ **S3 canceled 不可 retry**：`RETRY_ALLOWED_STATUSES` 移除 `canceled`（§4.1 终态不可恢复为 running，重新研究必须创建新 Task）；改写服务层与 API 层测试断言 409 E2003。④ **S4 取消检查点补齐**：`AgentLoop` 每轮迭代内每个 Tool 调用前检查取消（同迭代多 Tool 之间）；`fetcher` 每个 Web URL 抓取前、`renderer` Report 发布事务前经共享 `pipeline/cancel_guard.is_task_canceled` 检查，取消后停止抓取/抛 `TaskCanceledException` 不发布新 Revision（§13.2/§17.3-15）。⑤ **S6 hybrid 内部瞬时失败继续 Web**：`_run_knowledge_search(allow_empty=True)`，hybrid 内部通道全部 0 命中（非 fail-closed）不再抛 `SearchFailedException`，继续执行 Web 搜索，最终由完整度判定（§6.1/§12.2）；fail-closed（KB_FORBIDDEN/用户禁用/Contract 错误）仍立即中止。⑥ **S8 evidence_index 越界拒绝**：`_validate_indices` 与 claim 级 evidence_index 越界从过滤改为 `raise ValueError`（§8.1 严格引用闭包校验，§17.2-6 不生成虚假引用）；新增 claim 级 contradicts 门禁——`certainty=high` 且无 `qualification` 的 contradicts claim 拒绝（§9.5 门禁 5，不得合成为无条件确定结论）。⑦ **S9 重复关系合并**：`report_publisher._persist_claims_and_relations` 按 `(evidence_id, relation_type)` 去重合并、confidence 取高值（§9 门禁 4 / DATABASE.md §7.5 唯一约束），避免唯一约束 IntegrityError。⑧ **S10 引用闭包纵深防御**：`renderer` 加载 `evidence_by_id` 增加 `task_id` 过滤（§9 门禁 1）。验证：RED 逐项确认（S1 参数缺失、S2 `mark_budget_stopped` 缺失、S3 断言 409、S4 取消分支、S6 all_skipped raise、S8 越界未拒绝、S9 唯一约束冲突、S10 跨任务证据）；research 全量 unit（非 slow）1011 通过、1 跳过、1 failed（`test_llm` 本机 `.env` 覆盖 `LLM_MODEL` 的环境依赖，与本次无关）、契约 73 项 + `packages/contracts/tests` 131 项全绿、Architecture 16 项通过（1 项失败为本地未跟踪 `.opencode/package.json` 引入，与本次无关）；新增 `test_report_publish_duplicates` 1 项、S2 deadline 停止 3 项、S4 fetch/render 取消 2 项、S6 hybrid 继续 Web 1 项、S8 越界拒绝与 contradicts 门禁 5 项。（2026-08-07）
- 修复 M3 代码审查文档一致性缺口（纯文档变更，AGENTS.md 例外条款——不改变服务边界/公共契约/权限模型/数据生命周期，ADR 检查 1–8：否）：① `services/research/docs/DATABASE.md` §5.3 `research_steps` 表移除不存在的 `lease_generation` 列，明确所有权与提交门禁由 Task 级租约承载（`research_tasks.lease_generation` + `is_step_commit_allowed`），Step 身份由 `(task_id, sequence)` 与 `attempt_count` 表达；② §2.2 补充 `agent_memory_entries` 迁移态（保留用于内部断点续跑、`thought` 不外发，删除依赖 RESEARCH_PIPELINE §13.4 待后续切片，与 CHANGELOG 2026-08-06 切片 F 一致）；③ `docs/specs/API.md` §8.2 记录 `/api/v1/research` 端点覆盖迁移态（当前仅 `POST /tasks`，查询/cancel/retry/stream/state/report/delete 仍在 `/api/research`，目标态收敛到 `/api/v1/research/*`）；④ `services/research/docs/RESEARCH_PIPELINE.md` §10.1 登记 `report_publisher` 完整度近似口径迁移态（`total_steps` 近似 required 子问题、`channel_success` 有证据即成功，三分项持久化供 Resolver 预算停止判定，待完整通道计划替代）。均以交叉引用记录迁移事实、不重复定义技术事实。已执行格式与链接检查。（2026-08-07）
- 修复 M3 代码审查改进建议（均不改变服务边界/公共契约/权限模型/数据生命周期，ADR 检查 1–8：否）：
  ① **幂等指纹空白规范化**（对齐 API.md §8.1「规范化请求载荷计算指纹」）：`compute_request_fingerprint` 对 `topic` 先 `strip()` 再入指纹，与 `create_task` 落库口径一致——同 Key 载荷仅 topic 首尾空白不同视为重放（202，`idempotent_replayed=true`）而非 409 E2009。验证：RED 确认（纯函数 1 项 + API 层 1 项因指纹不一致失败）；新增 `TestComputeRequestFingerprint` 2 项与 API 用例 1 项，12 项全绿。
  ② **幂等并发 IntegrityError 误分类修复**（对齐 API.md §8.1 幂等语义）：`create_task_idempotent` 不再把全部 `IntegrityError` 按幂等竞争收敛，新增 `_is_idempotency_unique_conflict` 识别 `uq_research_tasks_user_idempotency`（MySQL 约束名 / SQLite 冲突列名双口径）；仅该约束冲突按重放收敛，其余（如 `research_task_knowledge_bases.selection_order`）上抛，避免误报 E2009。验证：RED 确认（非幂等约束冲突用例因误报 E2009 失败）；新增 `TestCreateTaskIdempotent` 2 项。
  ③ **`search_retrieval` 返回注解修正**：`app/core/internal_retrieval_client.py` `search_retrieval` 返回类型由 `-> dict` 修正为 `-> RetrievalSearchResult`（实际返回类型），消除类型标注与实际不符。
  ④ **knowledge/hybrid 预算 search_results 计数补齐**（对齐 RESEARCH_PIPELINE §14 结算口径）：`_settle_tool_budget` 同时识别 `total_internal_hits`（knowledge/hybrid 内部检索命中）与 `total_results`（web 搜索结果），避免 knowledge 策略 search_results 维度恒计 0。验证：RED 确认（新增用例断言 0≠7 失败）；新增 `test_runtime_budget.py` 1 项。
  ⑤ **预算停止事件幂等**（对齐 §14 一次受控停止一个事件）：`AgentRuntime._record_budget_stop` 增加实例级 `_budget_stop_recorded` 标记，维度超限在结算路径触发后，下一轮 `can_reserve` 抛 `BudgetExhaustedError` 进入 run() 分支时不再重复记录 `budget.stop`。验证：RED 确认（重复调用断言 2≠1 失败）；新增 `test_runtime_budget.py` 1 项。
  回归验证：本地 research 全量 unit（非 slow）988 passed / 16 failed 与 clean HEAD 失败集完全一致（16 项均为 TAVILY_API_KEY 未配置的既有环境依赖）；受影响套件 158 项全绿；容器内（挂载工作区，缺 aiosqlite 环境依赖仅影响 DB fixture 用例）非 DB 用例 51 passed；ruff check 与 ruff format --check 全部通过。（2026-08-07）
- 修复 M3 代码审查发现的三个严重问题（均为让实现重新符合既有权威规范，不改变服务边界/公共契约/权限模型/数据生命周期，ADR 检查 1–8：否）：
  ① **取消/租约竞态下报告仍发布的修复**（对齐 RESEARCH_PIPELINE §13.1/§13.2/§17.3-15、DATABASE.md §8）：`AgentRuntime._handle_fatal_error` 对 `LeaseLostError` 不再直接 return，而是先 `session.rollback()` 丢弃本会话已 flush 但未提交的写入（render 已 flush 的 `report_revision published`、Step 状态、预算结算、agent_events），避免 `_run_pipeline` 最终 commit 在租约失效/取消后仍落库；随后区分取消已提交（`cancel_requested_at` 非空）与真租约丢失——取消场景安全停止后经 `_finalize_task` 由 Resolver 推导 canceled/partially_completed 终态（不发布新 Revision），真租约丢失才交 Recovery Scanner 接管。验证：RED 确认（新增 2 项用例因 rollback/finalize 缺失失败）；新增 `test_runtime_lease.py` 2 项（取消已提交回滚并推导终态/真租约丢失回滚不推导终态），agent 与租约相关 90 项全绿。
  ② **SSRF IPv4-mapped IPv6 绕过修复**（对齐 RESEARCH_PIPELINE §6.3.2/§17.17）：`app/utils/url_safety.py` 内网黑名单补充 `::ffff:0:0/96`（IPv4-mapped IPv6，可映射回环/私网/链路本地/云元数据）与 `64:ff9b::/96`（NAT64 前缀）；已实跑复现 `http://[::ffff:127.0.0.1]` 可访问回环的绕过路径。验证：RED 确认（5 项新增用例失败）；新增 `test_fetcher.py::TestCheckUrlSafety` 5 项（`::ffff:127.0.0.1`/`::ffff:10.0.0.1`/`::ffff:169.254.169.254`/`::ffff:7f00:1`/`64:ff9b::a00:1` 均拒绝），14 项全绿。
  ③ **fail-closed 异常运行时吞错修复**（对齐 RESEARCH_PIPELINE §12.1/§12.2 fail-closed、ADR-010）：`app/core/exceptions.py` 新增 `FAIL_CLOSED_ERROR_CODES`（E1010/E3114/E3115/E3117）与 `is_fail_closed_error`；`AgentRuntime._execute_tool` 与 `PhaseHandlerTool.execute` 识别 fail-closed 异常后直接 re-raise（不再转 `ToolResult(success=False)` 继续循环、不再以 `E3999` 掩盖真实错误码），由 `run()` 的 `_handle_fatal_error` 保留错误码写 failed 终态，立即停止执行、不继续消耗预算。验证：RED 确认（2 项新增用例因异常被吞失败）；新增 `test_runtime.py::TestExecuteToolFailClosed` 2 项（E3115 立即重抛不吞/不以 success=False 继续），agent 与 tools 相关 87 项全绿。
  回归验证：本地 research 全量 unit（非 slow）977 passed / 16 failed 与 clean HEAD 失败集完全一致（16 项均为 TAVILY_API_KEY 未配置的既有环境依赖，与本修复无关）；容器内（挂载工作区）受影响套件 27 passed；ruff check 与 ruff format --check 全部通过。（2026-08-07）

- 修复 M2 审查 S1–S3 Provider 端点契约校验缺口（对齐 contracts/README.md §5、retrieval-request.schema.json、CHANGELOG「不做静默错误」）：正文 `contract_version` 与版本头不一致 → 400 `INTERNAL_CONTRACT_UNSUPPORTED`（search/resolve 双端点，新增 `_check_body_version`）；`query` 长度校验 1–8192（对齐 schema minLength/maxLength）；未实现过滤字段 `languages`/`updated_since`/`updated_until` 由静默忽略改为 400 `INTERNAL_CONTRACT_INVALID` 拒绝（`filters.document_ids` 语义保留）。修复 P1 chunk_count 计数膨胀：发布后不再 `+= len()` 累加，改为按 Active Version 重算（`versioning` 新增 `count_active_version_chunks`/`count_version_chunks`，`_embed_and_publish` 与崩溃恢复路径 `_run_publish_from_staging` 均回填；`_get_real_chunk_counts` 改为仅统计 Active Version chunks，排除旧版本）。修复 P3 发布 deleting 守卫（对齐 ADR-007）：KB 非 active 或文档 DELETING 时中止发布，置版本 failed 并抛 `PublishAbortedError`，tasks 两调用点捕获返回 failed，不再带病发布。修复 P5 resolve 不伪造定位：无 page 时优先 `metadata.section_path`（`>` 拆分、strip），均缺则 400 `EVIDENCE_SOURCE_UNAVAILABLE`，替换原伪造 `section_path=["来源"]`。验证：契约检索端点测试 25→34 全绿（新增正文版本不一致/空 query/超长 query/未实现过滤器/无定位 9 项）；versioning 15→19、ingest 72、services 194、vector_store 17、M1 核心回归 66 全绿。（2026-08-05）
- 修复 Research API 将任务投递到旧默认队列 `research_task`、而 Worker 仅监听 `research.execute` 导致任务未被拾取的问题；同时将定时任务显式路由到 `research.periodic`。（2026-08-02）
- 修复 Nginx 将旧 Research 任务路由 `/api/research` 错误转发到 Knowledge 或改写为 `/api/` 的问题；任务路径现原样代理，并保留命名空间下的 Research 健康检查与迁移期认证入口。（2026-08-02）
- 修复 Knowledge Celery 任务误投默认队列的问题，显式路由入库与删除任务；文档状态轮询增加请求防重叠及 429 限流退避。（2026-08-02）
- 移除无生产者的死队列 `research.recovery`（恢复任务继续投递到 `research.execute`），并统一队列配置键为 `CELERY_*` 惯例：补齐 `CELERY_PERIODIC_QUEUE`，Knowledge 队列改为 `CELERY_INGEST_QUEUE`/`CELERY_DELETE_QUEUE`，配置文档同步对齐。（2026-08-02）

### Refactored

- 将 `apps/web` 工程基线从 Vue 原地替换为 React + TypeScript（M4 切片 0，对齐 ADR-004、FRONTEND §2.2、UIDESIGN §4；仓库仍只有一个可运行前端）。删除 Vue/Pinia/Element Plus 源码与专属测试，建立 React 单根节点、Router/Provider/ErrorBoundary 分层、Vite React/TypeScript/Vitest/Testing Library/MSW/Playwright 工具链、TanStack Query 基线及 Tailwind Token 映射；完整落地浅色默认与深色可选的 `--es-*` 色彩、字体、圆角和焦点 Token，主题使用 `<html data-theme>` 与 `evidsight-theme` 持久化。依赖用途、维护与替代方案按获批 M4 方案登记；构建未产生第二前端目录。验证：Docker Node 22 容器内 React 基线测试 1 项全绿，`tsc --noEmit && vite build` 通过（初始基线产物 JS gzip 100.73KB）；屏蔽未跟踪本机 `.opencode/package.json` 后 Architecture 17 项全绿；Vue 源码、`.vue`、Pinia 与 plugin-vue 引用扫描无残留。ADR 检查 1–8：否（执行已接受 ADR-004 的前端原地替换，不改变服务边界、公共契约或数据生命周期）。（2026-08-08）

### Not Yet Implemented

- External OpenAPI（`docs/openapi/evidsight-v1.yaml`）尚未落地；Internal Contract Schema、Fixture 与生成物已随 M2 于 2026-08-04 落地。（2026-08-01）
- 本条目不声明统一身份、Internal Retrieval、Research 前端并入统一 Web、生产数据迁移或 v1.0 发布验收已经完成。（2026-08-02）

## 维护规则

- 行为、权限、状态机、公共契约和数据生命周期变化必须在同一变更中更新本文件。
- 变更分类与提交 subject 前缀对齐：`add`→`### Added`、`update`→`### Updated`、`fixed`→`### Fixed`、`refactor`→`### Refactored`；纯重构（外部行为不变、仅内部结构整理）必须记录在 `### Refactored` 下。
- `[Unreleased]` 下每个一级变更条目必须标注实际改动日期，格式为 `（YYYY-MM-DD）`；不得只依赖版本发布日期。
- 只记录实际合并的事实；测试结果必须保留实际命令、环境、日期和结果，不以计划目标代替执行记录。
- 发布版本使用 `MAJOR.MINOR.PATCH`，并标注日期、迁移要求、兼容窗口和已知限制。
